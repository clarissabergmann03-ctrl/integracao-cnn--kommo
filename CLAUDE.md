# CLAUDE.md

> Conhecimento **durável** do projeto (toda sessão lê isto). Status/tarefas ficam em `docs/fixes/BACKLOG.md`. Entradas curtas, sem logs. Atualize os gatilhos abaixo como hábito.

## O que é este sistema
Worker Cloudflare (Free) que integra **Clínica nas Nuvens (CNN) → Kommo CRM**, arquivo único `src/index.ts` + D1 + Cron (`* * * * *`). Reflete pacientes/agendas/orçamentos do CNN nas etapas dos funis do Kommo. Custo zero. **Direção unilateral CNN→Kommo** (lê CNN, escreve Kommo; nunca escreve CNN em produção — §7.8). Padrão: **produtores enfileiram** em `fila_trabalho` (D1), **consumidor drena** por tick. Sem webhook de orçamento no CNN → funciona por **polling/reavaliação** (relê o estado e reconcilia o card; a maioria dá `sem_mudança`).

## Mapa do código (`src/index.ts`)
> ⚠️ Os `~NNNN` são APROXIMADOS e desatualizam a cada edição — a **âncora real é o NOME da função** (dá `grep`). Não confie no número; confie no nome.
- **Cron / dispatcher**: `scheduled()` ~4157 (produtorVespera/Sync/Orcamento + consumirFila).
- **Fila (D1)**: `filaEnfileirarLote` ~511, **`filaClaimLote` ~545 (claim atômico UPDATE…RETURNING — caminho real)**, `filaPuxarPendentes` ~535 (peek só-leitura, só p/ dry-run), `filaMarcarFeito`/`filaMarcarErro`/`filaAdiar` ~565, `FILA_MAX_TENTATIVAS`=4 ~508, `FILA_LOCK_TTL_SEG`=300 ~530.
- **Consumidores**: `consumirFila` ~1966, `consumirItemA3` (sync agenda→etapa) ~1600, `consumirItemF2` (véspera/confirmação), `consumirItemOrcamento` ~1913, `consumirItemA4` (backfill).
- **Produtores**: `produtorSync` ~1494, `produtorVespera` ~1728, `produtorOrcamento` ~1823, `produtorBackfill`.
- **Reflexo de orçamento**: `decidirEtapaOrcamento` ~912 (puro), `orcamentosRecentes` ~930 (recência 60d), `temAgendaFutura` ~899 (portão agenda futura), `leadAlvoOrcamento` ~2016 (**seleção group-aware do card: APROVADO→B, demais→A**), `cnnOrcamentosDoPaciente` ~255, `get/upsertOrcamentoSync` ~938.
- **APIs**: `cnnGet/Post/Put` ~215-260, `assertCnnWritable` ~211 (bloqueia escrita CNN prod), `kommoGet/Patch/Post` ~298-332, `kommoThrottle`+`kommoNextSlot` ~289 (⚠️ global só-isolate), contador `subreqUsados/resetSubreq/orcamentoOk` ~53 (⚠️ global). **Retry (A4)**: `fetchComRetry` ~163 (backoff+Retry-After, marca `[TRANSITORIO]`), `ehTransitorio` ~160, `retryPadrao` ~206 (guarda `orcamentoOk`); todos os wrappers passam por ele.
- **Move/idempotência**: `moveLeadToStage` ~301 (guarda anti-Primeiro-Contato), `agenda_sync`/`mapeamento`/`orcamento_sync` helpers ~598-846, `audit()` ~568.
- **Webhooks/auth**: `handleLeadAgendado` (W1) ~867, `handleConfirmacao` (W2) ~943, `webhookAuthOk` ~3771 (`?secret=`), `discoverAuthOk` ~3774 (header).
- **Schema D1**: `ensureSchema` ~454 (tabelas: fila_trabalho +col `locked_at`, mapeamento, agenda_sync, orcamento_sync, auditoria, lembrete_d1, cursores…).
- **Lease de tick (B2)**: `adquirirLease`/`liberarLease`/`novoOwnerLease` ~509 (reusa `cursores.tick_lease`, `atualizado_em`=expira, `TICK_LEASE_TTL_SEG`=300); envolve `scheduled` e `/debug-tick` (incl. `clear=1`).
- **Debug (read-only)**: `/debug-audit`, `/debug-orcamento` (+`?decidir`/`?impacto`), `/debug-selftest` (logic|fuzz|stress), `/debug-retry-selftest` (A4, fetch mockado), `/debug-tick` (dry), `/debug-tick-log` (F1 — saúde por tick + backlog), `/debug-count` (contagem real Kommo por funil/etapa), `/debug-move`. **A5**: `/debug-fila-erros` (lista dead-letters), `/debug-fila-requeue?id=N|all=1` (dry por padrão; `&dry=0` reprocessa 'erro'→'pendente'/tentativas=0).
- **Migração (rotas, ver §Migração)**: `/mig-import?pagina=&rpp=&env=production` (CNN→D1), `/mig-reimport?limite=` (2º passe dos `erro_leitura`), `/mig-sync?limite=&dry=` (dreno D1→Kommo, idempotente), `/mig-criar-campos` / `/mig-campos-info` (campos "Inativo"/"tipo"), `/mig-verify-lead`, `/debug-migra-probe?pid=` / `/debug-migra-sweep?pagina=` (classificação read-only). Funções: `handleMigProbe`/`handleMigSweep` ~4454-4503, `ensureMigSchema` ~4560 (`mig_pacientes`/`mig_agendas`/`mig_orcamentos`/`mig_sync_log`), `classificarMigracao` ~4337 (puro), `migSyncBatch`/`migValidarLeadExistente`/`migMoveForwardOnly`/`migEnriquecer`.
- **Correções/manutenção (rotas)**: `/debug-split-colisao` / `/debug-consolidar-colisao` (colisão de telefone), `/debug-backfill-hist?fase=medir|consumir` (backfill histórico), `/debug-aniversario` (varredura), `/debug-mapa-campos`, `/debug-verificar`, `/debug-auditoria`, `/debug-migrar-mapeamento` (chave composta).

## Invariantes de negócio
- **0 duplicata DENTRO do mesmo funil** (não 2 cards A, nem 2 cards B, pro mesmo paciente).
- **Até 2 cards por paciente, 1 por grupo — INTENCIONAL** (confirmado dono 02/07): card A em Captação + card B em Pós-Venda podem coexistir (paciente com consulta/avaliação [A] E procedimento [B] em dias diferentes). **A mensagem do Salesbot é DIFERENTE por funil** → cada card precisa existir no seu funil. `mapeamento` PK = `(paciente_id_cnn, grupo)` + coluna `duplicata`; nome do card A ganha sufixo " (duplicata)". Os "124 pares A+B" da auditoria são isso, não bug. ⚠️ O bug real de identidade é a **colisão de telefone** (2 pacientes DIFERENTES → 1 lead).
- **CNN é a fonte da verdade**; Kommo é o espelho.
- **Move só quando o alvo muda** (idempotência via `*_sync.ultima_etapa` / `last_cnn_status`).
- Reflexo de orçamento só p/ **lead sem agenda futura** e em etapa "assentada" (`ETAPAS_ORC_PODE_AGIR`).

## Armadilhas conhecidas (gotchas)
- **`subreqUsados` é global de módulo** + `resetSubreq()` no topo do tick → NÃO confie sob concorrência (ticks sobrepostos corrompem o budget). **Mitigado (B2, 2026-07-02)**: lease cooperativo (`adquirirLease`/`liberarLease`, linha `cursores.tick_lease`, TTL 300s) serializa cron + `/debug-tick` → não há mais 2 ticks sobrepostos. (Fan-out real ainda exigirá budget local — B1.)
- **Fila COM claim atômico (C1, 2026-07-02)**: `filaClaimLote` reivindica em 1 instrução (`UPDATE…WHERE id IN(SELECT…LIMIT)…RETURNING`), marca `status='processing'`+`locked_at`, `tentativas+1`; drenos concorrentes puxam disjuntos (D1 single-writer). Reclaim de `processing` preso > `FILA_LOCK_TTL_SEG`. `adiado`→`filaAdiar` (volta a pendente e desfaz o incremento). ⚠️ `filaMarcarErro` NÃO incrementa (o claim já fez); `filaPuxarPendentes` é só peek de dry-run. Fan-out ainda bloqueado por OUTROS itens (subreqUsados global etc.).
- **Chave de fila A3 é permanente** → reversão de status (confirma→desconfirma) é deduplicada pra sempre → move perdido.
- **Colisão de telefone**: `phoneKey` = DDD+8 dígitos; 2 pacientes CNN com mesmo telefone colapsam num lead (4 casos reais).
- **ORC group-aware + cria card B (corrigido 02-03/07)**: `leadAlvoOrcamento` escolhe o card pelo FUNIL da decisão — **APROVADO→Pós-Venda=card B**; **ABERTO/CANCELADO/PERDIDO→Pós-Consulta=card A**. Card-alvo inexistente: **ABERTO/CANC sem card A → pula**; **APROVADO sem card B → CRIA o card B** (Opção 2 do dono) em Pós-Venda/Tratamento Iniciado, sem agenda (dados do paciente vêm do orçamento), via `criarCardLead` (agora suporta card sem agenda: só ID Paciente CNN, sem AGENDAMENTO/ID Agenda, sem agenda_sync). Idempotência ANTES do create (respeita move/delete manual). `acharLeadDoPaciente` REMOVIDO. **Testado REAL** (wrangler dev --remote, Kommo real, paciente teste 28146949): Kommo aceita o card sem-agenda, nasce em Tratamento Iniciado, mapeamento(pid,B) gravado. Observabilidade: `/debug-orcamento?paciente=X&cardalvo=1` (D1) e `&aplicar=1&dry=1` (roda o ORC de 1 paciente, dry por padrão). **NO AR** (validado ao vivo: A7 adotou sem duplicar, ORC moveu venda real).
- **Kommo NÃO permite DELETE de lead (405, testado 03/07)**: `kommoDelete("/leads/{id}")` → 405 Method Not Allowed. Não há hard-delete de lead pela API v4; limpeza de card é manual (UI) ou movendo pra etapa "perdido". `kommoDelete` só serve p/ outras entidades.
- **Reflexo ORC descarta APROVADO com `dataAprovacao` > 60d**; **ABERTO não tem data** no objeto CNN → não é filtrável por recência (o sweep pega abertos antigos).
- **Kommo `filter[status_id]` é SILENCIOSAMENTE IGNORADO** → usar `filter[statuses][0][pipeline_id]&[status_id]` + guarda em código + paginação.
- **Kommo 204/corpo vazio** quebra `res.json()` → `kommoGet` trata (retorna `{_embedded:{}}`).
- **`143` = status "perdido" genérico** em TODO funil → sempre escopar por `pipeline_id`.
- **429/503/502/504 + throw de rede AGORA têm retry/backoff (A4, 2026-07-02)**: `fetchComRetry` repete (teto 4, respeita Retry-After, backoff exp., espera ≤4s, guarda `orcamentoOk`). Esgotou → erro marcado `[TRANSITORIO]` → `consumirFila` chama `filaAdiar` (NÃO queima tentativa). Permanente (4xx) → `filaMarcarErro` (conta). Selftest: `/debug-retry-selftest`.
- **CNN não tem webhook de orçamento** → só polling. **Kommo TEM webhook.**
- **Token CF é IP-restrito** e o IP do ambiente oscila → `wrangler` falha com `9109`/`7403` (confirmado ainda em 05/07: `deployments list`/`d1 execute` barram deste IP). **Fallback confiável = endpoints HTTP `/debug`** com `WEBHOOK_SECRET` no header `Authorization` (funciona sempre; não depende do IP). O `WEBHOOK_SECRET` de teste está em `.claude/settings.local.json` (permissões de comando). Ex. de saúde ao vivo: `curl -H "Authorization: <WEBHOOK_SECRET>" .../debug-tick-log`.
- **Kommo rate limit ≈ 7 req/s por conta**; `kommoThrottle` só coordena dentro de um isolate.
- **ORC drena ~0,6 move/min** (bucket-2, rebaixado); backlog cresce na varredura diária e drena depois (transitório).

## Como testar com segurança
- **NUNCA** dispare efeito irreversível: WhatsApp real, mover etapa de lead de paciente vivo, criar card real. Salesbot/gatilhos devem estar OFF ao testar.
- **CNN produção = SÓ LEITURA** (§7.8). Escrita CNN só no **sandbox** (`?env=sandbox`).
- **Registro de teste**: TESTE Bruno — Paciente CNN `28146949`, tel `11946800329` (+ lead/contato Kommo, convênio `24808788`). Telefones de teste: `11946800329`/`92982717586`/`92994567328`.
- **Local**: `npx wrangler dev` + `curl localhost:8787/debug-selftest?mode=logic|fuzz|stress` (precisa `.dev.vars` com `WEBHOOK_SECRET`; apagar depois).
- **Edge sem deploy**: `npx wrangler dev --remote` (secrets reais) → dados reais read-only.
- **Dry-run em prod**: `/debug-tick?...&dry=1` (não escreve) e `/debug-orcamento?impacto=1` (mede).

## Decisões de arquitetura (data | decisão | porquê)
- 2026-06-29 | Flip para fila/dispatcher; aposentar crons legados C1/C2 | diluir carga em micro-ticks sob o teto de 50 subreq.
- 2026-07-01 | Reflexo de orçamento CNN→Kommo (APROVADO→Tratamento Iniciado etc.) | refletir vendas sem webhook do CNN.
- 2026-07-01 | Recência de 60d no reflexo de orçamento | CNN não expõe status de execução (dadosExecucao=null) → recência é o único proxy; não reativar tratamento antigo/inativo.
- 2026-07-01 | Reativar "Perdido" com orçamento aprovado | cliente que comprou = venda recuperada (mal-rotulado pelo backfill).
- 2026-07-02 | Fan-out/sharding PAUSADO | auditoria achou bugs de concorrência/idempotência bloqueadores; corrigir antes.

## Comandos úteis
- Deploy: `export CLOUDFLARE_API_TOKEN="$(tr -d '[:space:]' < '../acesso Cloudflare Worker Token Api.txt')" && npx wrangler deploy`.
- **✅ NO AR: `56e818e1` (03/07)** — TODAS as correções da auditoria + evolução do ORC, validadas ao vivo. Inclui: C1 (claim atômico) + A4 (retry, POST só-429) + A5 (dead-letter) + B2 (lease) + C1-TTL(90s) + ORC group-aware + create-B + A7 (adoção anti-duplicata) + BX1/BX2 + A1a + A3 + F1-log + F1-alerta + A2. Histórico de versões e rollbacks: ver `docs/fixes/BACKLOG.md` (rollback seguro pré-tudo = `b0167bfa`).
- **Health ao vivo**: `/debug-tick-log` (F1 — saúde por tick + backlog por tipo/idade) ou D1: `SELECT ok,gatilhos,erros,ms,fila_pendente FROM tick_log ORDER BY id DESC LIMIT 5`.
- **F1-alerta**: `verificarAlertaGrave` no `finally` do `scheduled` cria **tarefa no Kommo p/ Marcos Venâncio (user 11348335, Follow-up)** SÓ em erro grave — (A) 3 ticks seguidos `ok=0` OU (B) `fila.erro >= 25` — cooldown 6h (`cursores.ultimo_alerta_grave`). Testado real (tarefa criada, HTTP 200).
- **A2 (colisão de telefone/família), no ar `56e818e1`**: 2 partes. (1) `marcarFamiliaSeColisao` (~1599, no vínculo A4/A3): card compartilhado por 2+ pacientes CNN → acrescenta " [Familia]" ao nome (1x, idempotente). NÃO divide card. (2) **Guard na véspera** (`consumirItemF2` ~2072): card `[Familia]` (2+ pacientes no MESMO lead, detecção `COUNT(DISTINCT paciente_id_cnn) FROM mapeamento WHERE lead_id_kommo` ≥2, sem subrequest) **NÃO vai pra Confirmação de Consulta** (retorna `pulado_familia`) — fica em Consulta Agendada; o atendente confirma à mão (regra do dono: 1 caso só, sem função/tarefa). Testado real (marca) + D1 (detecção n=2 vs 1).
- Verificar sem WEBHOOK_SECRET: `npx wrangler d1 execute kommo-cnn-db --remote --json --command "..."` (usa o token). Ex.: `SELECT status,COUNT(*) FROM fila_trabalho GROUP BY status`; `SELECT funcao,acao,COUNT(*) FROM auditoria WHERE ts>strftime('%s','now')-3600 GROUP BY 1,2`.
- Testar código NOVO contra Kommo real (sem deploy): `wrangler dev --remote` + `.dev.vars` só com `WEBHOOK_SECRET` de teste (override; CNN/Kommo vêm do remoto). Probe A7: `/debug-lookup-paciente?pid=X&grupo=B`.
- Logs ao vivo: `npx wrangler tail` (efêmero — sem histórico).
- D1: `npx wrangler d1 execute kommo-cnn-db --remote --json --command "SELECT ..."` (⚠️ token oscila).
- Estado vivo (sem token): `curl -H "Authorization: $WEBHOOK_SECRET" https://kommo-cnn.clarissabergmann03.workers.dev/debug-audit`.
- Pausar ORC: `ORC_ENABLED=false` + redeploy.

## Entendimento do projeto (visão narrativa)
→ `docs/ENTENDIMENTO-PROJETO-2026-07-02.md` (síntese de todos os .md do projeto cruzada com este CLAUDE.md — ⚠️ escrito em 02/07, ANTES dos deploys de correção; a parte "duplicata não deployada / produção card-único" está SUPERADA: o ORC já é group-aware e o modelo de 2 cards está no ar). Achados-chave AINDA válidos: **roteamento é SEMPRE pelo TIPO da agenda, nunca por "já é cliente"**; os **124 pares A+B** são duplicata intencional (família/consulta+procedimento), não bug.

## Estado dos reparos da auditoria (03/07)
**TODOS concluídos e no ar** (`56e818e1`), cada um testado antes de subir (vários com teste real no Kommo via `wrangler dev --remote`). Detalhe item-a-item + histórico de versões/rollback: `docs/fixes/BACKLOG.md`. **Design de cada correção** (por quê/como): `docs/fixes/specs/` (spec-A7/A3/A1a/BX/C1ttl/F1log). **CONGELADOS** (a re-validação provou que dependiam de fan-out pausado / webhooks inertes — NÃO construir): A6, B1, BX4, E45. **Pendências do dono**: confirmar rotação do token CF (se foi só IP) + trashar card de teste 19416346.
> **Onde está o "backup":** o código vivo e completo é o próprio `src/index.ts` (deployado). Rollback = `wrangler` pelos Version IDs no BACKLOG (NÃO por cópias locais). Os snapshots `scratchpad/index.after-*.ts` eram rede de segurança temporária DA SESSÃO — descartáveis, redundantes com o `src/index.ts` final. Projeto não é git; se quiser versionar de verdade, `git init` + commit do `src/` + `docs/`.

## Migração Sync Única (one-time CNN→Kommo) — DEPLOYADA E EXECUTADA (~04/07; verificado ao vivo 05/07)
> **STATUS (05/07, verificado ao vivo):** a migração JÁ RODOU. Base Kommo cresceu de **~3624 (03/07)** para **~8153**: Captação **6981** (perdido 5083 / 1º contato 1320 / consulta agendada 153 / …), Pós-Consulta **760** (em análise 171 / perdido 589), Pós-Venda **412** (cliente ativo 299 / tratamento iniciado 64 / …). `/mig-sync?dry=1` retorna **0 pendente** → a fila de staging D1 já drenou. Arquitetura final = **D1 staging** (CNN→D1→Kommo), NÃO o `wrangler dev --remote` direto: `/mig-import` grava a base no D1 (`mig_pacientes`), `/mig-sync` dreno idempotente escreve no Kommo. ⚠️ Pico transitório de "Workers errors" no dashboard (visto 05/07) veio do **driver da migração martelando os `/mig-*`** (leitura CNN sob concorrência), NÃO do cron (tick_log = 60/60 ok). Agora estamos em **modo manutenção** (bugs + pequenas features).
> Migração **ÚNICA e IRREVERSÍVEL** de todo o histórico (6 anos) do CNN para as etapas certas do Kommo. NÃO é recorrente; roda como **processo SEPARADO em paralelo** ao Worker de prod. Plano vivo + critérios de aceite: `docs/migracao/BACKLOG-MIGRACAO.md`; arquitetura D1 staging: `docs/migracao/D1-STAGING.md`.
- **Separação/não-sobreposição (regra do dono 03/07):** a migração NÃO roda dentro do Worker de produção — não compartilha isolate/globais (`subreqUsados`/`kommoNextSlot`/lease) → sem colisão com o cron; e foge do teto de **50 subreq/request** (que é por-request do CF Worker). Partição natural por tempo: o **cron** cuida da JANELA CORRENTE (~agenda −7d…+90d + orçamentos 60d); a **migração** cuida do HISTÓRICO FRIO (sem agenda futura, última atividade meses/anos atrás) → domínios disjuntos. Bloco Futuro (agenda ≥ 2026-07-01) é domínio do cron; a migração não escreve nele. Idempotência: ler o lead no Kommo (ID Paciente CNN + etapa atual) ANTES de mover; toda escrita via `mapeamento` + `moveLeadToStage` (trava anti-rebaixamento) → overlap acidental é seguro.
- **Janelas (dono 03/07):** ATIVA/futura = agenda `data` ∈ [2026-07-01, 2028-01-01] → Bloco Futuro (vence tudo). HISTÓRICO = [2020-07-03 (HOJE−6a), 2026-07-01). Limiares: aprovação recente = **2 meses**; reset = **9 meses**; silêncio/abandono = **180 dias**.
- **Etapas-alvo — TODAS já existem (verificado /discover ao vivo 03/07):** Captação(13847079): consulta agendada `106848619`, perdido `143`. Pós-Venda(13950431): tratamento iniciado `107658907`, cliente ativo `107658911`, abandono `107774015`, cancelamento/reembolso/ocorrências `107774019`, concluído `142`, perdido `143` (+bônus não usados por padrão: saldo pendente `107658915`, recorrência/manutenção `107774023`). Pós-Consulta(13947295): em análise `107633739`, perdido `143`.
- **Viabilidade (cruzado c/ API CNN):** execução de tratamento NÃO vem da API (`dadosExecucao`=null; sem %/flag por item). "Fez todos os procedimentos?" = **DERIVADO**: `orcamento.procedimentos[].idTipoProcedimento`+quantidade (do aprovado) ⊆ procedimentos de agendas grupo-B **FINALIZADO** pós-aprovação. "Cancelou após aprovar" = orçamento `dataAprovacao`≠null E status atual ∈ {CANCELADO,PERDIDO}. Orçamento só traz `dataAprovacao` (sem data de criação) → RESET 9m é **aproximado** (ordem de `id` + janela CRIACAO; indeterminado → revisão manual). Enumeração da base = `/paciente/lista` paginado. Enriquecimento Kommo existente: Aniversário(nascimento), Faciais, Corporais, contato, ID Paciente CNN — NÃO há campo Origem nem valor de orçamento.
- **Escala:** Kommo = 3624 leads (Captação 2798 / Pós-Venda 667 / Pós-Consulta 159); base CNN a enumerar ~alguns milhares. Escrita limitada por Kommo **~7 req/s** (teto externo, não paralelizável) → agentes aceleram leitura/classificação/validação, não a escrita.
- **Classificação = CÓDIGO determinístico, NUNCA LLM por-paciente** (regra §0.3 do spec). Agentes paralelizam build/validação/dry-run/auditoria — não a decisão. Sempre: **dry-run total → validação amostral → piloto ~100 (verifica CADA card) → run cheio resumível**, log por lead, checagem por lote.
- **Arquitetura (dono 03/07): `wrangler dev --remote`** (caminho 2). Endpoint de migração no código, rodado via preview isolada → isolate/globais separados do cron (sem colisão; cron não dispara em preview), reusa os secrets já na CF (write-only, não dá pra copiar). Vale o teto de 50 subreq/request → **micro-lotes (~1 paciente/request) + cursor no D1 + driver local em loop** (log + verificação por lote). Risco: token CF oscila → resume/cursor.
- **Escopo = PASSE DE CORREÇÃO COMPLETO (dono 03/07):** não é só histórico frio — reclassifica TODA a base para a etapa certa, corrigindo mis-placements em qualquer funil (ex.: em "tratamento iniciado" com agenda → "cliente ativo"; em "primeiro contato" com agenda futura → "consulta agendada"). No Bloco Futuro move só **forward-only** (nunca rebaixa; respeita a ordem das etapas) → não briga com o cron.
- **GRUPO A/B calibrado na PRODUÇÃO (/discover?env=production 03/07):** A = Consulta/Avaliação(66666), Atendimento Social(66671). B = Pequenas Cirurgias(66667), Encaixe(66668), Encaminhamento-INTERNO(66669), Procedimento(66670), RETORNO(66672), Cortesia(67118), Cirurgia(93892). Bate com `GRUPO_A/B_TIPOS` do código.
- **Caso parcial (dono 03/07):** APROVADO >2m, fez PARTE dos procedimentos (não concluiu/cancelou/abandonou) → **revisão manual** (default). "Saldo pendente" = deve dinheiro, não é o caso parcial (e provavelmente não é detectável — sem financeiro na API).

## 2 Webhooks Kommo→CNN (escrita guardada) — GO-LIVE: LIGADOS + ESCRITA EM PRODUÇÃO (07/07)
> Feature nova: webhooks que ESCREVEM no CNN a partir de eventos do Kommo (o polling CNN→Kommo continua intacto). Spec: `docs/superpowers/specs/2026-07-05-kommo-cnn-3-webhooks-escrita-cnn-design.md`.
> **ESTADO (07/07): NO AR e LIGADO — Version `dd7d1ac0`, `WH1_ENABLED=1` + `WH2_ENABLED=1` + `CNN_WRITE_TARGET=production`** (no `wrangler.toml [vars]`). Escreve no CNN REAL. Rollback: `d34c37a6` (pré-webhooks) ou setar flags=0. WH1 (confirmar) age em confirmações reais; **WH2 (agendar) fica dormente até a secretária preencher o campo "Tipo Procedimento CNN"** (campo novo, ninguém preencheu ainda). Cron 60/60 ok, 0 dead-letters no go-live.
> **✅ VALIDADO EM PRODUÇÃO COM ESCRITA REAL (07/07):** um paciente REAL (lead Kommo `19762726`) confirmou → WH1 escreveu `CONFIRMADO_PACIENTE` na **agenda real `130161758`** (2026-07-07, atendimento social, 18h) — **verificado direto no CNN prod** (`/debug-agendas?env=production`). Anti-loop suprimiu **4 re-disparos reais** (`suprimido_ja_no_alvo`), 0 dead-letters, cron 60/60 ok, backlog vazio. Também: E2E completo em SANDBOX (WH1+WH2) + dry-run em prod. **Feature 100% no ar e provada.** (WH2 segue dormente até secretária usar o campo "Tipo Procedimento CNN".)
> **VALIDADO AO VIVO EM SANDBOX (07/07, via `wrangler dev --remote` + deploy de teste):** tsc 0 novos; selftest 52/52+23+7; 2 rounds de certificação multi-agente (19 achados). **Webhook 1 confirmar E2E:** agenda `130143931` → CONFIRMADO_PACIENTE ✓ + anti-loop suprimiu re-disparo (`ja_confirmado`) ✓. **Webhook 2 agendar E2E:** criou agenda nova `130143935` no sandbox (tipo Encaixe) ✓ + gravou ID no card ✓ + idempotência (2º disparo `ja_agendado_mesmo_ts`, SEM duplicata) ✓ + erro tratado (tipo inválido → dead-letter, sem duplicar) ✓. 0 dead-letters ao fim.
> ⚠️ **Teste no fluxo real precisa do código DEPLOYADO** (o cron precisa conhecer os tipos `CNN_*`; senão os come como "tipo_desconhecido"). `wrangler dev --remote` divide a fila/cron de PRODUÇÃO com o worker no ar → não drena os tipos novos sozinho.
> ⚠️ **Webhook 3 (remarcar) REMOVIDO (06/07, decisão do dono):** remarcação é ação do CNN → já refletida automaticamente pela REGRA 1 (`consumirItemA3` ~2309: hora muda → atualiza card). Um webhook Kommo→CNN de remarcar não tem gatilho real (ninguém remarca editando campo do card). Removidos: rota `/webhook/remarcar`, `handleRemarcar`, `consumirItemCnnRemarcar`, tipo `CNN_REMARCAR`, `WH3_ENABLED`, `faltaUmDia` (o "falta 1 dia → tarefa" foi dropado junto).
- **Rotas (2):** `/webhook/confirmacao` (W2→estendido, tipo `CNN_CONFIRMAR`, gatilho: card→Consulta Confirmada), `/webhook/pos-venda-agendar` (`CNN_AGENDAR`, gatilho: card pós-venda→Cliente Ativo com AGENDAMENTO+Tipo). Setup: `/wh-criar-campo`. `?dry=1` em ambos (sem efeito colateral — audit também guardado por `!dry`).
- **Guardrail (§7.8 vira allowlist):** `cnnProducaoPermitido(method,path,status?)` só libera em produção `POST /agenda/novo`, `PUT /agenda/alteracao-status` (status ∈ {AGENDADO,CONFIRMADO_PACIENTE}), `POST /agenda/{id}/remarcar` (mantido p/ o `syncKommoParaCnn` inerte), `POST /convenio-paciente/associar`. DELETE/`/paciente/*`/orçamento continuam LANÇANDO. Card `[Familia]` → recusa escrita.
- **Anti-loop:** reusa `agenda_sync` (+col `origin`); `decidirSupressao` (puro) por `(last_agendamento_ts ±60s, last_cnn_status)` — origin é só observabilidade. Handlers purgam gêmeo terminal (`purgarGemeoFeito`, feito/erro) antes de enfileirar. `CNN_AGENDAR` tem idempotência anti-duplicata (agenda_sync + `acharAgendaCnnPorHorario`, ambos fail-closed). `leadEhFamilia` fail-closed (erro D1 → lança `[TRANSITORIO]` → retenta).
- **Campo novo Kommo (JÁ CRIADO, field id `2440080`):** select "Tipo Procedimento CNN" (opções: Procedimento/Cirurgia/Pequenas Cirurgias/Encaixe/Retorno/Cortesia/Encaminhamento-INTERNO). **⚠️ O `idTipoConsulta` é resolvido pelo NOME no ambiente de ESCRITA** (`resolveTipoConsultaId` via `resolveTiposConsulta`) — NÃO por id chumbado, porque **sandbox×prod têm IDs distintos** (ex.: Encaixe = 110454 sandbox vs 66668 prod) e o CNN pode renumerar. `TIPO_PROCEDIMENTO_CNN` (ids de prod) agora só VALIDA que o valor é um tipo conhecido. **Corrigido no teste ao vivo** (07/07): a 1ª versão chumbava o id de prod → dead-letter no sandbox ("tipo não existe").
- **Ferramenta de teste:** `/test-workflow?acao=set-tipo-proc&tipo=Encaixe&data=&hora=&clear_agenda=1` (seta o campo Tipo + AGENDAMENTO + limpa ID Agenda, p/ testar o WH2). Só telefones de teste.
- **Guia da atendente:** `docs/PLAYBOOK-ATENDENTE.pdf` (+ fonte `.html`) — guia de usuário do fluxo híbrido.
- **Webhooks p/ o dono configurar no Kommo:** `POST .../webhook/confirmacao?secret=<WEBHOOK_SECRET>` (etapa Consulta Confirmada) e `POST .../webhook/pos-venda-agendar?secret=<WEBHOOK_SECRET>` (etapa Cliente Ativo), corpo `leads[status][0][id]=<id>`.
- **GO-LIVE (passos do dono):** já feito = deploy + `/wh-criar-campo` + validação ao vivo em sandbox. **Falta só:** no `wrangler.toml [vars]` setar `CNN_WRITE_TARGET="production"` + `WH1_ENABLED="1"` + `WH2_ENABLED="1"` → `wrangler deploy` → observar `/debug-audit` + `/debug-fila-erros`. Pode ligar gradual (só WH1 primeiro). ⚠️ Com flags ON + sandbox, confirmações reais geram dead-letter (agenda real tem id de prod, não existe no sandbox) — por isso o estado seguro fora do go-live é flags OFF.

## MIGRAÇÃO github/vercel/supabase (EM ANDAMENTO — hábito: atualizar a CADA avanço de código/deploy)
> Stack nova = **Vercel Functions (Node)** = lógica + **Supabase Postgres** = banco + **pg_cron/pg_net** = cron 1min → `/api/tick` + **GitHub** repo `clarissabergmann03-ctrl/integracao-cnn--kommo`. **CF SEGUE EM PRODUÇÃO até o cutover.** Plano: `docs/superpowers/plans/2026-07-06-plano-github-vercel-supabase.md`. Estado detalhado na memória `[[migracao-supabase]]`.
> Deploy Vercel atual (preview público): `kommo-h6a6dmm8f-…vercel.app` (selftest 57/57). App em `app/` (subdir isolado do worker legado `src/`).

**FEITO:**
- **Fase 1 Fundação** ✅ — Supabase `qfyivaualpgqxjbrrleo` (sa-east-1), 9 tabelas + pg_cron/pg_net, camada `app/lib/db.ts` (postgres.js, pooler `aws-1-sa-east-1:6543`, `prepare:false`, `?`→`$n`).
- **Fase 2 Porte** ✅ — `src/index.ts`→`app/lib/core.ts` (`handleFetch`/`handleScheduled`), `api/index.ts` (roteador) + `api/tick.ts`, `lib/env.ts` injeta DB. Selftests 57/57 (52+5 guardrail) local (tsx) e no deploy. Dialeto: `ensureSchema`→no-op, `INSERT OR IGNORE`→`ON CONFLICT`, imports `.js`.
- **Dados migrados** ✅ — 6 tabelas de idempotência (mapeamento 1556, agenda_sync 628, orcamento_sync 1237, lembrete_d1 146, cursores 3, agendamento_sync 5). auditoria/tick_log/fila NÃO migrados.
- **RLS** ✅ (crítico) — habilitado nas 9 tabelas + revoke anon/authenticated (anon LIA PII → agora `42501`). App via role `postgres` bypassa. Migration `20260707020000_rls_seguranca.sql`.
- **Guardrail** ✅ — jamais deletar/alterar paciente no CNN, QUALQUER ambiente (`assertCnnWritable` absoluto + `cnnDelete` sempre lança + 5 selftests).

**FALTA (cutover + validação):**
- **Cutover:** pg_cron→`/api/tick` (token no Vault), re-apontar webhooks Kommo p/ Vercel, promover prod (URL estável), desligar cron do CF.
- **Validação write end-to-end (paciente teste 11946800329):** BLOQUEADA — paciente **não existe no CNN produção** (0 agendas/0 orçamentos p/ 28155333 e 28146949; IDs no lead Kommo 17488447 = resíduo sandbox). Precisa fixture: paciente+agenda REAIS no CNN produção.
- Secrets CNN sandbox não subidos (eram do ambiente de teste).

**🔴 ACHADOS CRÍTICOS (fixture paciente de teste em produção, 07/07):**
- **Constantes de criação de agenda são de SANDBOX — falham em PRODUÇÃO.** Descoberto ao montar o fixture; valem para o Worker CF (`src/index.ts`) TAMBÉM. Mapa sandbox→produção (lidos do CNN prod real):
  | Constante (code) | sandbox | **produção** |
  |---|---|---|
  | `CNN_CONVENIO_PARTICULAR` | 56545 | **27603** (nomeTipoConvenio "Particular") |
  | `CNN_TIPO_CONSULTA` | 110452 | **66666** Consulta/Avaliação · 66671 Atend. Social · 66672 RETORNO · 66670 Procedimento · 93892 Cirurgia · 66668 Encaixe |
  | `CNN_LOCAL_AGENDA` | 41170 | **19775 / 19779** (salas reais) |
  | `CNN_TIPO_PROCEDIMENTO` | 1011844 | outros (ex. 381357 "Av Botox"); há ~100 tipos |
  → **Implicação REFINADA (verificado no código 07/07):** o caminho REAL de agendar em produção é `consumirItemCnnAgendar` (fila, `wt=cnnWriteTarget=production`), disparado por `/webhook/pos-venda-agendar` (W2, WH2_ENABLED=1). Nele:
    - `idTipoConsulta` é **resolvido por NOME no alvo** (`resolveTipoConsultaId`→`resolveTiposConsulta(prod)`); se não existir, PULA. ✅ **sem bug** (pega o id de produção certo).
    - `idPacienteConvenio` tenta associar 56545 (falha em prod) mas **cai no convênio já existente do paciente** → ✅ p/ paciente real com convênio; ❌ p/ paciente SEM convênio.
    - `idLocalAgenda: CNN_LOCAL_AGENDA=41170` e `procedimentos:[{idTipoProcedimento: CNN_TIPO_PROCEDIMENTO=1011844}]` são **hardcoded de SANDBOX** (linhas 1181-1182) → **não existem em produção → o POST /agenda/novo FALHA (400)**. ❌❌
    - → Ou seja: **agendamento pós-venda em produção falha hoje** por `idLocalAgenda`+`idTipoProcedimento` sandbox (NÃO por "falta de procedimento" — o procedimento é enviado, só é um id inválido). Provável que NENHUM pós-venda tenha sido criado com sucesso em prod desde o go-live 06/07 → checar `/debug-fila-erros`.
    - **`/webhook/lead-agendado` (`handleLeadAgendado`, agenda Grupo A) é LEGADO e escreve em SANDBOX** (`cnnPost` sem target = default sandbox, linha 1338) — nunca toca produção; usa constantes sandbox (válidas lá). A **confirmação** (`consumirItemCnnConfirmar`, PUT alteracao-status) NÃO manda tipo/local/proc → sem esse bug, funciona em produção.
    - **✅ CORRIGIDO (07/07, commit bdafc15):** `cnnConvenioParticular`/`cnnLocalAgenda`/`cnnTipoProcedimento(env,target)` — target-condicionais. Produção overridável por env (`CNN_CONVENIO_PARTICULAR_PRODUCTION`/`CNN_LOCAL_AGENDA_PRODUCTION`/`CNN_TIPO_PROCEDIMENTO_PRODUCTION`); defaults = **27603 / 19775 / 381357** (lidos do CNN prod). Usado em `getOrCreateConvenioParticular` + `consumirItemCnnAgendar`. +7 selftests (logic 64/64). `handleLeadAgendado` (W1 legado/sandbox) e `handleDebugCriarAgenda` seguem sandbox (correto). **⚠️ Falta o dono confirmar a SALA e o PROCEDIMENTO reais** que a clínica quer nos agendamentos automáticos de pós-venda (os defaults 19775/381357 são os que descobri; setar via env na Vercel antes do go-live de pós-venda). **Aplicar o mesmo fix no Worker CF `src/index.ts` no cutover.**
- **Regras do `POST /agenda/novo` (produção, confirmadas):** procedimento é **obrigatório** p/ QUALQUER tipo (inclusive Atendimento Social); horário **fora do expediente** é bloqueado (03:00 e 07:00 → "fora do horário de atendimento"); conflito de sala/horário é validado (ex. sala 19775 já ocupada 10:00/12:30 em 07/07).
- **Filtros do `/paciente/lista`:** `nomeContem` FUNCIONA; `telefoneCelularContem` e `limite` são **IGNORADOS** (voltam sempre a 1ª página genérica). Dedup de paciente por telefone tem que ser **client-side** (buscar por nome/lista e casar `fxTelKey` do telefone) — NÃO confiar no filtro do servidor. Isso afeta o dedup por telefone do sistema (linha ~2799 `telefoneCelularContem`).
- **3 pacientes DUPLICADOS criados em produção** (artefato das tentativas que falhavam na agenda após criar o paciente): "Bruno �[Teste]" ids **28524071, 28524126, 28524693**, todos tel `11946800329`. NÃO deletáveis (guardrail). Inertes (sem agenda). Remoção manual na tela do CNN se o dono quiser. O endpoint `/debug-fixture-teste` agora deduplica por telefone → reusa o 28524071, não cria mais.
- **Endpoint criado:** `/debug-fixture-teste?modo=probe|convenios|tipos|criar` (escopo TRAVADO ao tel/lead de teste; auth `Authorization: WEBHOOK_SECRET`). `criar` aceita `?convenio=&tipoConsulta=&localAgenda=&tipoProc=&hora=&data=&nome=&forcar=1`. Só cria paciente com `&forcar=1` (fetch direto — única exceção autorizada ao §7.8; guardrail global intacto).
- **✅ Fixture CONCLUÍDO (07/07):** paciente teste **28524071** (reusou órfão, sem nova duplicata) + agenda **130276667** (08/07 11:30, AGENDADO, tipo 66666 Consulta/Avaliação=Grupo A, sala 19775, convênio 27603) no CNN produção, vinculada ao lead **17488447** (F.Captura/Consulta Agendada). CONFIRMADO por leitura no destino real (`modo=verificar`): agenda_cnn.status=AGENDADO + lead.id_agenda_cnn=130276667. Endpoint `/debug-fixture-teste` modos: probe|convenios|tipos|verificar|criar.

**✅ VALIDAÇÃO E2E (07/07, deploy kommo-3z86tfi67):**
- **F.Captura — confirmação Kommo→CNN (CNN_CONFIRMAR): PROVADO.** `/webhook/confirmacao` (lead 17488447) → fila → `/debug-tick?dry=0` (consumidor, escreve via cnnWriteTarget=produção) → **agenda 130276667 no CNN: AGENDADO→CONFIRMADO_PACIENTE** (confirmado por leitura real, não pela resposta). Idempotência: 2ª chamada → `suprimido: ja_confirmado` (decidirSupressao viu agenda_sync CONFIRMADO_PACIENTE) → tick processou 0, 0 PUT duplicado. Latência ~2,4s. Taxa 2/2.
- **F.Pós-venda — agendar Grupo B (CNN_AGENDAR): PROVADO.** Fila CNN_AGENDAR (payload leadId/pid/ts/tipoNome, igual ao `/webhook/pos-venda-agendar`) → `/debug-tick?dry=0` → `consumirItemCnnAgendar` criou **agenda 130276669 no CNN produção** (AGENDADO, 09/07 12:30, **idTipoConsulta 66668=Encaixe resolvido por nome**, **idLocalAgenda 19775 = valor de produção CORRIGIDO**, convênio 27603), vinculada ao lead. Confirmado por leitura real. **⇒ o fix de config funciona no caminho REAL** (não só no endpoint de fixture). Idempotência: reenfileirar mesmo ts → `suprimido: ja_agendado_mesmo_ts`, contagem de agendas no slot = 1 (0 duplicata). Descoberto: `tipoNome=Retorno` é rejeitado pelo CNN ("fora do prazo de reconsulta") — regra de negócio do CNN, tratada como erro permanente (transitorio:false), NÃO é bug nosso. **⇒ ambas as escritas Kommo→CNN (confirmar + agendar) provadas e2e + idempotentes; taxa 4/4.**
- **Régua D-1 (CNN→Kommo): PROVADA e2e + idempotente (07/07, cutover-janela).** Sequência reversível: (1) re-sync de estado D1→Supabase (upsert; evita re-envio de lembrete); (2) **pausa do cron do CF via API** (`PUT .../workers/scripts/kommo-cnn/schedules []`, reversível); (3) `produtorVespera(prod, 2026-07-08, soPid=28524071)` escopado ao teste → 1 F2 → consumidor F2 non-dry **moveu o lead 18149444: "Consulta Agendada"(106848619)→"Confirmação de Consulta"(107785399)** — confirmado por leitura real do Kommo; (4) **restaura cron do CF** (schedules=`* * * * *`, verificado). Idempotência: re-rodar → `enfileirados=0 ja_lembrado=1` (pula, 0 duplicata). **⇒ direção CNN→Kommo (régua/pooling→consumidor→escrita Kommo) provada e2e.**
- **⚠️ DUPLICAÇÃO DE CARD descoberta:** o paciente de teste 28524071 tem **3 leads Kommo** (17488447 = meu fixture manual; 18149444 e 19416346 = criados pelo pooling do CF). Causa: o fixture manual (escrevi ids CNN no lead) **não criou `mapeamento`** → o pooling do CF não reconheceu o card e criou paralelos. Confirma que **`mapeamento` é o guarda anti-duplicata** (agora re-sincronizado D1→Supabase). Em operação normal o W1 cria o mapeamento. Artefato de teste; limpar cards extras manualmente se quiser.
- **Pré-cutover — re-sync de estado (07/07):** `scripts/resync-estado-d1.mjs` (upsert D1→Supabase). Supabase = D1 vivo: cursores 3, lembrete_d1 146, agenda_sync 636, mapeamento 1561, orcamento_sync 1237, agendamento_sync 5. **Garante que a Vercel não re-execute ações** (lembrete D-1, varredura de cursor). Re-rodar antes do go-live definitivo (D1 muda enquanto o CF vive).
- **Reflexo de orçamento (Pós-Consulta):** paciente de teste tem 0 orçamentos e `POST /orcamento/novo` é bloqueado pelo allowlist → **e2e não validável sem fixture manual de orçamento no CNN**. Lógica (`decidirEtapaOrcamento`) coberta por selftest.
- **ESTRESSE:** `/health` (sem DB) 50 concorrentes → **100%**, p50 0,47s (camada de função escala ✅). `/debug-audit` (4 queries Supabase) 40 concorrentes → **80–97,5%** (varia), 1–8 req penduram **60s (504=maxDuration)**. **CAUSA RAIZ (confirmada): espera por conexão do pooler** — NÃO é query lenta (tuning idle/max_lifetime/connect/statement_timeout + max=8 em `db.ts` NÃO resolveu; 504s persistem). É **teto do Supavisor free-tier (pool de servidor ~15)** sob burst concorrente pesado. **Não corrigível só em código no plano free** → exige Supabase pago (pool maior) ou reduzir queries/req. **Carga real (cron 1/min + webhooks esparsos) fica MUITO abaixo desse burst**, então não é problema prático — mas a barra "≥99,5% sob estresse de 40 concorrentes pesados" NÃO é atingível no free-tier. Tuning de `db.ts` mantido (higiene: evita hang de conexão morta em operação normal).
- **RESTRIÇÃO CRÍTICA — CF ainda vivo:** os fluxos **Kommo→CNN** (confirmação, pós-venda agendar) são **patient-scoped** (webhook+fila) → testáveis isoladamente na Vercel. Mas os produtores **CNN→Kommo** (produtorSync/Backfill/Vespera/Orcamento = pooling, régua D-1, reflexo de orçamento) **varrem JANELAS, não são patient-scoped** → rodá-los na Vercel enquanto o CF está vivo **DUPLICA** trabalho (bancos separados D1×Supabase → cada um cria a task/lembrete D-1 → task duplicada no Kommo). ⇒ **o e2e da direção CNN→Kommo exige o CUTOVER** (só 1 sistema drenando). Pré-cutover, só dá pra validar a LÓGICA (selftests/dry-run), não o e2e com escrita.
- **Orçamento (Pós-Consulta):** e2e exige um orçamento de teste no CNN, mas `POST /orcamento/novo` é (corretamente) bloqueado pelo allowlist → precisa criação manual no CNN ou validação só-lógica (decidirEtapaOrcamento é selftestado).

**Aprendizados Vercel/Supabase:** imports `.js` (NodeNext — `.ts` dá `ERR_MODULE_NOT_FOUND`); `typescript` nas devDeps faz a Vercel type-checar (TS2347 = warnings não-fatais); `ssoProtection:null` via API p/ público; env via API `/v10/projects/{id}/env?upsert=true`; pooler = `aws-1-sa-east-1` (não aws-0); token CF IP-restrito (IP de saída 200.149.56.26, oscila).

## Trabalho em andamento
> **Fase atual (05/07): MODO MANUTENÇÃO** — correções de bugs, coisas que não funcionam e pequenas funcionalidades. Migração já deployada/executada e cron saudável (60/60 ticks ok, 05/07). Hábito acordado com o dono: **atualizar este CLAUDE.md continuamente conforme as descobertas** (bugs achados, correções, features novas, estado do deploy).
→ 2 Webhooks Kommo→CNN (confirmar + agendar pós-venda; remarcar removido): CONSTRUÍDO+CERTIFICADO, **deploy pendente** + ativação gradual (ver §acima).
→ Migração Sync Única: DEPLOYADA/EXECUTADA (ver §Migração acima) — `docs/migracao/BACKLOG-MIGRACAO.md` (log 03/07 ainda não registra o deploy final; código é a fonte da verdade).
→ Correções da auditoria de 2026-07-02: `docs/fixes/BACKLOG.md` (todas FEITAS).
