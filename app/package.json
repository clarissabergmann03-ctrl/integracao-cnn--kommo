{
  "name": "kommo-cnn-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "kommo-cnn na Vercel (Functions Node) — integração CNN↔Kommo",
  "scripts": {
    "test": "node --env-file=../.env --test lib/*.test.ts"
  },
  "dependencies": {
    "postgres": "^3.4.5",
    "@vercel/functions": "^2"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22"
  }
}
