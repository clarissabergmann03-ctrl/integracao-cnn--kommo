{
  "name": "kommo-cnn-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "kommo-cnn na Vercel (Functions Node) — integração CNN↔Kommo",
  "scripts": {
    "test": "node --import tsx --env-file=../.env --test lib/*.test.ts"
  },
  "dependencies": {
    "postgres": "^3.4.5",
    "@vercel/functions": "^2"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.19.0"
  }
}
