# BACKLOG — Correções da auditoria (2026-07-02)

> Estado temporal. Conhecimento durável = ../../CLAUDE.md. **WIP=1** (nunca 2 itens em-andamento). Ordem por dependências. Não reescreva histórico — só adicione/mova status. Status: backlog → pronto → em-andamento → testado-local → deployado → FEITO.

## AGORA
Fase 1 em execução autônoma (deploy + rotação = gated). WIP=1. Itens deployáveis vão sendo **testado-local**; deploy em lote quando o dono liberar.
- **C2/C3** — parte autônoma FEITA (docs limpos, grep=0). Falta ROTAÇÃO (pendência humana). Ressalva C2: token ainda vive em workflow scripts (~/.claude) + transcript → só rotação neutraliza.
- **C1 — testado-local ✅** (não deployado). Claim atômico `filaClaimLote` + reclaim TTL (`FILA_LOCK_TTL_SEG`=300) + `filaAdiar` + `filaMarcarErro` sem duplo-incremento; `filaPuxarPendentes` virou peek de dry-run. Provado no D1 local: 2 claims disjuntos cobrindo 10; tentativas+1 exato; reclaim; adiar desfaz; dead-letter≥4. Snapshot: `scratchpad/index.after-C1.ts`.
- **A4 — testado-local ✅** (não deployado). `fetchComRetry` (backoff+Retry-After, marca `[TRANSITORIO]`, guarda `orcamentoOk`) nos 7 wrappers CNN/Kommo; `consumirFila` roteia transitório→`filaAdiar` / permanente→`filaMarcarErro`; endpoint `/debug-retry-selftest`. Provas: 11/11 cenários no `wrangler dev --local` (HTTP 200, pass:true) + D1 (transitório tentativas=0/pendente, permanente=1/pendente). Snapshot: `scratchpad/index.after-A4.ts`.
- **A5 — testado-local ✅** (não deployado). 2 endpoints aditivos (sem migração): `/debug-fila-erros` (lista dead-letters) + `/debug-fila-requeue?id=N|all=1` (dry por padrão, escopo `status='erro'`, zera tentativas/ultimo_erro). Provas via `wrangler dev --local`: total=3+campos+boom+401; id-dry não escreve; id-commit→pendente/0/null; all&dry=0→requeued:3 com feito intacto=2. Regressão: selftest logic 33/33 + retry-selftest pass. Snapshot `scratchpad/index.after-A5.ts`.
- **B2 — testado-local ✅** (não deployado). Lease cooperativo (`adquirirLease`/`liberarLease`, reusa `cursores.tick_lease`, TTL 300s) envolve `scheduled` + `/debug-tick` (incl. `clear=1`); release no `finally`. Provas: CAS SQL determinístico (A vence/B perde/expira→C vence/release só do dono); endpoint — lease futuro→skip+processados=0; clear=1 com lease→fila intacta; lease expirado→adquire+dreno; 2 ticks simultâneos sobre 10 itens→soma=10, 10 feito (sem duplo). Regressão: logic 33/33, retry pass, A5 ok. Snapshot `scratchpad/index.after-B2.ts`.
- **ORC-GA (group-aware) — testado-local ✅** (não deployado). **Bug do que está NO AR**: o ORC usava `acharLeadDoPaciente` (card A primeiro) → num paciente com duplicata (A+B) arrastava o card de Captação pro funil errado. Fix: `leadAlvoOrcamento` escolhe o card pelo funil da decisão (APROVADO→card B; ABERTO/CANCELADO/PERDIDO→card A); card-alvo inexistente → PULA (Opção 1 do dono, ORC não cria card). `acharLeadDoPaciente` removido. Corrigido também no `/debug-orcamento-impacto`. Novo `/debug-orcamento?paciente=X&cardalvo=1` (só-D1). Provado local: P1 duplicata→B=BBB/A=AAA; P2 só-A→B=null(pula)/A=A2; P3 só-B→B=B3/A=null. Regressão logic 33/33 + retry pass. Snapshot `scratchpad/index.after-ORC-groupaware.ts`.
- **PRÓXIMO PASSO EXATO:** restantes precisam de você OU de decisão: A6 (G, dep A4, decompor), B1 (G, dep A4/A6/A5/C1, decompor), A1/A7 (G, decompor), A2/A3 (M — cuidado com lead vivo), E45 (G, webhook HMAC — coordenar Kommo), F1/F2 (observabilidade), BX (dep B1). Recomendo: **deployar+verificar o lote** e então atacar A6/B1 decompondo.
- **NOTA (git):** projeto NÃO é repo git → sem `commit` por item; registro = este BACKLOG + snapshots (`scratchpad/index.after-<ID>.ts`). Deploys **gated** (peço go); vão em lote.
- **ORC create-B (Opção 2) + A4 POST-fix — testado ✅ (inclui REAL no Kommo, 03/07).** APROVADO sem card B → CRIA o card B (Pós-Venda/Tratamento Iniciado, sem agenda). `criarCardLead` suporta card sem agenda. A4: `kommoPost`/`cnnPost` só re-tentam 429 (não duplicam card). Novo `/debug-orcamento?paciente=X&aplicar=1` (roda ORC de 1 paciente, dry padrão). **Teste real via `wrangler dev --remote`** (Kommo real, paciente 28146949): card sem-agenda aceito, nasce em Tratamento Iniciado, só ID Paciente CNN, mapeamento(pid,B) gravado (idempotência ok). retry-selftest 17/17 (6 casos POST). Snapshot `scratchpad/index.after-createB-tested.ts`. **Achado:** Kommo não permite DELETE de lead (405). **Pendência: apagar à mão o card de teste lead 19416346** (inerte — mapeamento já removido).
- **REVALIDAÇÃO (workflow adversarial 02/07):** C1 é redundante-em-efeito com B2 (fan-out pausado) + tem regressão fina de hard-crash → MANTER sem urgência (mitigar TTL depois). A4/B2/A5/ORC-GA sustentam. Itens pendentes: **congelar A6/B1** (dependem de fan-out pausado), **fundir F2 em F1**, **adiar E45** (webhooks inertes); reais = **A2 > A3 > F1 > A1a > A7** (A7 elevado pelo A4 POST).
- **✅ DEPLOYADO (03/07, Version `7560f9c0`):** [C1(+TTL 90s), A4(+POST-fix), A5, B2, ORC-GA, create-B, A7]. Validado AO VIVO: migração `locked_at` ok; fila drenando/0-erro; **A7 `adotado_por_pid` (sem duplicata)**; **ORC `etapa-movida`** (venda real). Impacto medido: ~11 criaria_b + 6 move de 379 aprovados. A7 provado real (`/debug-lookup-paciente` + adoção do card teste). Rollback: `b0167bfa`; kill-switches: lease `DELETE FROM cursores WHERE nome='tick_lease'`, ORC `ORC_ENABLED=false`. Snapshot `scratchpad/index.after-A7-C1ttl.ts`.
- **✅ ROUND 2 DEPLOYADO (03/07, Version `da84e50f`):** BX1 (audit dead-letter) + BX2 (expõe orcamento_sync) + A1a (guard: não reconcilia órfão sob paginação parcial) + A3 (purga gêmeo 'feito' → reversão de status re-enfileira) + F1-log (`tick_log` durável + `/debug-tick-log`). Testados: BX/A1a type-clean; A3 SQL (BUG feito=1/pendente=0 → FIX pendente=1 → IDEMP pendente=1); F1 local (cron simulado → tick_log ok=1 + endpoint) + AO VIVO (1º tick prod: ok=1, gat=orc, 0 erros). Specs em `scratchpad/specs/`; snapshot `scratchpad/index.after-round2.ts`. A2 **deferido** (decidido, sem spec — ver abaixo).
- **CONGELADOS (a re-validação provou que dependem de fan-out pausado / webhooks inertes):** A6, B1, BX[4] (fan-out), E45 (webhooks W1/W2 não configurados). Não construir agora.
- **A2 — DECIDIDO (03/07), implementação deferida:** na colisão (2 pacientes CNN diferentes → mesmo telefone → 1 lead), marcar o card com **nome do principal + " (Família)"** (idempotente). Não colapsa/duplica; sinaliza. Deferido de propósito (sem spec; toca o caminho de vínculo `acharLeadPorTelefone` no A4/A3 → merece spec+teste próprios pra não regredir). ~4 casos reais, baixa urgência.
- **✅ F1 alerta externo DEPLOYADO (03/07, Version `0220c70c`):** tarefa no Kommo p/ **Marcos Venâncio** (user 11348335) só em erro GRAVE (3 ticks `ok=0` seguidos OU `fila.erro>=25`), cooldown 6h. `verificarAlertaGrave` no `finally` do `scheduled`. Testado real (tarefa criada HTTP 200); pós-deploy: ticks ok=1, cursor de alerta vazio (nada disparado). Canal escolhido pelo dono = tarefa Kommo.
- **Housekeeping:** card de teste lead 19416346 (consistente após adoção A7; trashar à mão se quiser). Token CF: doc `../acesso Cloudflare Worker Token Api.txt` (⚠️ token vivo em texto puro — não commitar; confirmar se foi Roll ou só IP).

## PENDÊNCIAS HUMANAS
- **C2 — rotacionar token Cloudflare** (doc já limpo). VOCÊ: dashboard CF → Manage Account → API Tokens → o token de deploy (Workers+D1) → **Roll** (mantendo a MESMA IP allowlist) → me devolver o novo valor (ou usá-lo você no deploy). Eu verifico: token antigo `/tokens/verify` = `success:false`; `deploy --dry-run` com o novo = ok.
- **C3 — rotacionar WEBHOOK_SECRET** (doc já limpo). VOCÊ: `npx wrangler secret put WEBHOOK_SECRET` com valor aleatório ≥32 chars + atualizar o `?secret=` do webhook no Kommo. Me avisar. Eu verifico: `/discover` com secret antigo = 401 / novo = 200; cron intacto.
- **A8 — DECIDIDO (02/07):** a pergunta virou de "precedência orçamento×agenda" para o modelo de cards. Dono confirmou: **modelo de DUPLICATA é intencional** — 1 paciente pode ter card A (Captação) + card B (Pós-Venda) ao mesmo tempo, porque **a mensagem do Salesbot difere por funil**. Roteia-se SEMPRE pelo TIPO da agenda. O ORC deve agir no card do funil da sua decisão (APROVADO→B, ABERTO/CANCELADO/PERDIDO→A), pulando se o card não existe (Opção 1). → implementado em **ORC-GA** (acima). A pergunta original da recência (60d p/ CANCELADO/PERDIDO?) segue em aberto, mas de baixo impacto.

## APRENDIZADOS
- **C1 (2026-07-02):** o claim atômico introduz um risco sutil — se o `budget` de subrequests estoura no meio do lote, os itens já reivindicados (status='processing') mas não processados ficariam presos até o TTL (300s) e com `tentativas` queimada. Corrigido liberando a CAUDA via `filaAdiar` no `break`. Lição: **todo ponto de saída antecipada de `consumirFila` precisa devolver o que reivindicou** (vale p/ futuros early-returns). Antes (peek) isso era grátis porque nada era marcado.
- **C1:** concorrência real de D1 é single-writer → teste sequencial de 2 claims já **prova** a disjunção (o 2º não vê como reivindicável o que o 1º pegou); não precisei de 2 processos simultâneos de fato.
- **Modelo de duplicata (02/07):** os docs se contradizem (escopo-mãe/ESPECIFICACAO dizem "0 duplicata, B-vence"; redesign 29/06 introduz 2 cards). A **verdade é o dono + o código deployado**: o modelo de 2 cards (1 por grupo) É intencional e a camada de dados (mapeamento PK `(paciente,grupo)`) + A3/A4/F2 já o implementam. O ORC ficou pra trás (escrito p/ card-único) → foi o único a corrigir. Lição: quando docs divergem, **ler o código deployado + confirmar o intent com o dono**, não seguir o doc. Entendimento completo em `docs/ENTENDIMENTO-PROJETO-2026-07-02.md`.

---

## Fila ordenada
| # | ID | Grupo | Título | Esf. | Deps |
|---|----|-------|--------|------|------|
| 1 | C2 | Credenciais | Rotacionar token Cloudflare vazado no dashboard e substituir por placeholder no doc | P | — |
| 2 | C3 | Credenciais | Rotacionar WEBHOOK_SECRET e remover o valor literal dos docs | P | — |
| 3 | C1 | Concorrência | Claim atômico (lock) na fila de trabalho: puxada com UPDATE...RETURNING | M | — |
| 4 | A4 | Concorrência | Retry/backoff em 429/503/rede nos wrappers CNN e Kommo (parar de dead-letterar por rate-limit transitório) | M | — |
| 5 | A6 | Concorrência | Coordenar throttle do Kommo entre invocações (token bucket durável + backoff 429) | G | A4 |
| 6 | A5 | Perda de dado | Dead-letter observável + requeue controlado para itens 'erro' da fila_trabalho | M | — |
| 7 | B2 | Concorrência | Adicionar lease cooperativo em D1 para serializar ticks do cron e jobs /debug | M | — |
| 8 | B1 | Concorrência | Tornar o orçamento de subrequests local por invocação (remover contador módulo-global) | G | A4, A6, A5, C1 |
| 9 | A1 | Perda de dado | Não reconciliar órfãos A quando a paginação do produtorSync foi parcial (e auto-curar card em Perdido com agenda vigente) | G | — |
| 10 | A2 | Perda de dado | Guard de colisão de telefone ao vincular: não colapsar 2 pacientes CNN num lead | M | — |
| 11 | A3 | Perda de dado | Fila A3: impedir dedup permanente de reversão de status (purgar 'feito' ou rotacionar chave) | M | — |
| 12 | A7 | Perda de dado | Tornar recuperação de criação de card resiliente a duplicata (lookup por ID Paciente CNN) | M | A4, A6 |
| 13 | A8 | Demais | Corrigir recência do reflexo de orçamento que rebaixa venda real do pós-venda | P | — |
| 14 | E45 | Segurança | Assinar webhooks (HMAC + janela timestamp/nonce) e tornar handlers idempotentes | G | — |
| 15 | F1 | Observabilidade | Registrar cada tick de forma durável em D1 (status/erro/contadores) + alerta externo no catch e habilitar [observability] | M | — |
| 16 | F2 | Observabilidade | Persistir sumário por tick (tick_resumo) + expor backlog da fila por tipo/status/idade | M | — |
| 17 | BX | Baixo (lote) | Polir lote de 6 fragilidades BAIXAS independentes (observabilidade + segurança + consistência) | M | B1 |

---

## Itens

### C2 — Rotacionar token Cloudflare vazado no dashboard e substituir por placeholder no doc
- **Origem**: achado C2 (CRITICO) | Evidência: docs/superpowers/plans/2026-07-01-reflexo-orcamento.md:153 — linha exata: `$env:CLOUDFLARE_API_TOKEN="<TOKEN-CF-ANTIGO>"`. Grep no repo (excl. node_modules) confirma que este é o ÚNICO local com o valor real; demais docs usam `<token>`. Secundário: Account ID `4be8e1b3bbf4d7074f665e77fd6dca2d` em ESPECIFICACAO-TECNICA.md:412.
- **Problema**: Token de API da Cloudflare (`cfut_...`) gravado em texto puro num doc de plano. Se o doc vazar, dá controle da conta CF (D1, deploy do Worker, secrets). Dispara AGORA: o segredo já está exposto em disco e no histórico da sessão; qualquer cópia/backup/leak do repositório expõe a conta. Mitigante parcial: token é IP-restrito (erro `9109` fora do IP allowlisted), o que reduz mas não elimina o risco.
- **Critérios de aceite**:
  - [ ] Token antigo invalidado: `curl -s https://api.cloudflare.com/client/v4/user/tokens/verify -H "Authorization: Bearer <TOKEN-CF-ANTIGO>"` (rodado do IP allowlisted da Bruno) retorna `"success":false` (código 1000 / token invalid). Antes da correcao o mesmo comando retorna `"success":true`.
  - [ ] Sweep de segredo zerado: `grep -rniE "cfut_[a-z0-9]{20,}" D:/clarissa-bergmann/kommo-cnn --include='*.md' --include='*.ps1' --include='*.toml' --include='*.ts' -r` (fora de node_modules) retorna 0 linhas; especificamente a linha 153 do doc passa a conter `<token>` (ou `$env:CLOUDFLARE_API_TOKEN` referenciando store seguro) e nenhum literal `cfut_`.
  - [ ] Deploy continua funcional com o NOVO token: `$env:CLOUDFLARE_API_TOKEN="<novo>"; npx wrangler deploy src/index.ts --dry-run` conclui sem erro de auth (sem `code 9109`/`10000`), provando que a rotacao nao quebrou o pipeline de deploy.
  - [ ] Sweep tambem cobre `.wrangler/` e o scratchpad da sessao: `grep -rniE "cfut_[a-z0-9]{20,}"` nesses caminhos retorna 0 matches (nenhuma copia residual do valor antigo).
- **Plano de teste**:
  - (a) repro: (a) `grep -rn "cfut_" docs/superpowers/plans/2026-07-01-reflexo-orcamento.md` mostra o token em texto puro na linha 153. Opcional (prova que esta VIVO, read-only): `curl -s https://api.cloudflare.com/client/v4/user/tokens/verify -H "Authorization: Bearer <TOKEN-CF-ANTIGO>"` do IP allowlisted -> `"success":true`. Raio de dano: chamada /tokens/verify e GET puro, NAO altera estado da conta CF.
  - (b) prova: (b) Acao humana no dashboard CF (Manage Account > API Tokens): fazer Roll do token (mesmo id, novo valor) OU delete+recreate mantendo o mesmo escopo/IP allowlist. Depois: editar doc linha 153 para `<token>`. Verificar: (1) verify com token ANTIGO -> `"success":false`; (2) `grep -rniE "cfut_[a-z0-9]{20,}"` no repo (excl. node_modules), em `.wrangler/` e no scratchpad -> 0 matches; (3) `wrangler deploy --dry-run` com o NOVO token -> ok.
  - (c) regressão: (c) Entorno: `npx wrangler deploy src/index.ts --dry-run` com o novo token conclui sem erro de auth (pipeline de deploy intacto); `npx wrangler dev --local` sobe e D1 local responde a um SELECT trivial (`wrangler d1 execute <db> --local --command "SELECT 1"`) — confirma que dev local NAO depende do token e nada mais quebrou. Conferir que nenhum outro placeholder `<token>` nos demais docs foi alterado.
- **Dependências**: nenhuma
- **Risco do deploy**: Baixo. Rotacionar invalida imediatamente o token antigo: qualquer automacao/CI que ainda use o valor antigo para de autenticar ate ser atualizada — hoje o deploy e manual (Bruno roda wrangler local com env var), entao basta usar o novo token no proximo deploy. Cuidado 1: identificar o token CERTO no dashboard (por id/nome) para nao derrubar outro token da conta. Cuidado 2: recriar mantendo a MESMA IP allowlist, senao o proximo deploy falha com `9109`.
- **Rollback**: Nao ha "un-rotate": se o novo token falhar (ex.: IP allowlist errada -> `9109`), recriar outro token com o mesmo escopo e IP correto. A edicao do doc e trivialmente reversivel, mas NAO reverter para o valor em texto puro — o valor antigo deve permanecer morto. Nenhum estado do Worker/D1 e alterado por esta tarefa.
- **Esforço**: P | **Status**: backlog
- **Notas**: Nao e um repo git (verificado: `git rev-parse` -> not a git repo), logo NAO ha historico git para purgar — remediar = rotacao + editar 1 linha + sweep. O segredo tambem existe no historico desta sessao/transcript do Claude, que nao da para grepar/limpar; por isso a ROTACAO (nao a edicao do doc) e o que neutraliza o risco — a edicao e higiene. Secundario (fora do escopo estrito de C2, mas anotar): Account ID `4be8e1b3bbf4d7074f665e77fd6dca2d` em ESPECIFICACAO-TECNICA.md:412 nao e segredo por si so, mas reduz defense-in-depth quando somado ao token; opcional trocar por placeholder. Independente de A4/A6 (wrappers Kommo), A5/C1 (fila) e B1 (budget) — nao toca codigo do Worker. CNN prod read-only nao se aplica (token e da Cloudflare, nao do CNN); a checagem /tokens/verify e GET read-only na API da CF.

### C3 — Rotacionar WEBHOOK_SECRET e remover o valor literal dos docs
- **Origem**: achado C3 (CRITICO) | Evidência: docs/superpowers/plans/2026-07-01-reflexo-orcamento.md:62,83,102,120 (literal `<WEBHOOK_SECRET-ANTIGO>` no header `Authorization` dos curls). Consumido em src/index.ts:3771 (webhookAuthOk, `?secret=` — protege webhooks W1/W2 nas rotas index.ts:4147/4150/4151) e src/index.ts:3774 (discoverAuthOk, header `Authorization` — protege ~35 rotas `/debug-*` + `/discover` + `/test-workflow`). grep do valor no repo retorna SÓ essas 4 linhas (não está no bundle .wrangler; secret é injetado em runtime).
- **Problema**: O WEBHOOK_SECRET (`<WEBHOOK_SECRET-ANTIGO>`) — credencial única que autentica TODOS os `/debug-*`, `/discover`, `/test-workflow` e os webhooks W1/W2 — está em texto puro no doc do plano. Qualquer pessoa com acesso ao arquivo tem acesso admin total ao Worker (rodar drenos, mover leads, disparar webhooks). Dispara sempre que o doc é lido/compartilhado; agravado por não ser rotacionado.
- **Critérios de aceite**:
  - [ ] grep -ri "<WEBHOOK_SECRET-ANTIGO>" em D:/clarissa-bergmann/kommo-cnn retorna 0 ocorrências (valor removido de todos os arquivos); as 4 linhas do doc passam a usar o placeholder `$SECRET` (padrão já usado em 2026-06-28-fase1 e 2026-06-29-fase2)
  - [ ] Segredo rotacionado: `npx wrangler secret list` mostra WEBHOOK_SECRET presente E `curl -s -o /dev/null -w "%{http_code}" -H "Authorization: <WEBHOOK_SECRET-ANTIGO>" https://kommo-cnn.clarissabergmann03.workers.dev/discover` retorna 401, enquanto o mesmo curl com o NOVO valor retorna 200
  - [ ] Webhooks re-protegidos sem quebrar: `POST .../webhook/confirmacao?secret=<WEBHOOK_SECRET-ANTIGO>` retorna 401 e o mesmo POST com o NOVO secret retorna != 401 (200), confirmando que a config de webhook do Kommo foi atualizada junto
  - [ ] Cron intacto: `npx wrangler tail` durante 2 ticks do cron `* * * * *` após a rotação mostra 0 erros novos (scheduled() não usa WEBHOOK_SECRET) e `/debug-audit` com o NOVO secret retorna 200
- **Plano de teste**:
  - (a) repro: (a) BUG: `grep -rn "<WEBHOOK_SECRET-ANTIGO>" D:/clarissa-bergmann/kommo-cnn` → hoje retorna 4 hits (linhas 62/83/102/120 do plano) = segredo em texto puro. Provar que é credencial viva: `curl -s -o /dev/null -w "%{http_code}" -H "Authorization: <WEBHOOK_SECRET-ANTIGO>" https://kommo-cnn.clarissabergmann03.workers.dev/discover` → 200 (admin). Raio de dano: /discover é GET read-only, não escreve nada — seguro.
  - (b) prova: (b) LOCAL primeiro (sem tocar prod): `npx wrangler dev` com `.dev.vars` contendo `WEBHOOK_SECRET=<novo-aleatorio>`; `curl -w "%{http_code}" -H "Authorization: errado" http://localhost:8787/discover` → 401 e com o valor certo → 200 (valida o mecanismo de auth). Depois, na prod (após OK do dono): `npx wrangler secret put WEBHOOK_SECRET` com valor aleatório 32+ chars; editar o doc trocando `<WEBHOOK_SECRET-ANTIGO>` por `$SECRET`. Verificar: (1) grep do valor = 0 hits; (2) curl /discover com valor ANTIGO → 401; (3) com valor NOVO → 200; (4) `POST .../webhook/confirmacao?secret=<WEBHOOK_SECRET-ANTIGO>` → 401.
  - (c) regressão: (c) ENTORNO: confirmar que W1/W2 e o cron seguem vivos. `npx wrangler tail` por 2 ticks do cron → 0 erro novo. Rodar TESTE Bruno (paciente CNN 28146949, tel 11946800329) por `/test-workflow` com o NOVO secret → 200 e fluxo normal no /debug-audit; reverter qualquer movimento de lead depois (limpar após teste). Disparar 1 webhook de teste com o fixture Bruno e o NOVO secret → confirma que a config atualizada no Kommo aceita o novo valor. Raio de dano: usar sandbox/fixture Bruno; CNN prod é só-leitura; reverter etapa via /debug-move.
- **Dependências**: nenhuma
- **Risco do deploy**: ALTO se descoordenado
- **Rollback**: `npx wrangler secret put WEBHOOK_SECRET` de volta ao valor conhecido restaura a auth em segundos; reverter a URL de webhook no Kommo para o secret anterior. Guardar cópia do doc antes de editar (repo não é git — sem histórico). A edição do doc é cosmética e reversível.
- **Esforço**: P | **Status**: backlog
- **Notas**: Escopo de C3 = SOMENTE WEBHOOK_SECRET. O mesmo doc vaza, na linha 153, também um CLOUDFLARE_API_TOKEN (`cfut_qWgu...`) em texto puro — é um achado SEPARADO (provável C-outro), não dependência de C3; mas a mesma passada de scrub no arquivo deve tratar ambos. Sem dependência de A4/A6 (wrappers Kommo), A5/C1 (fila) ou B1 (budget): a auth é isolada. Dependência OPERACIONAL (não-código): atualizar a config de webhook do Kommo com o novo secret na mesma janela da rotação. Observação: o plano 2026-06-28-fase1-sync-confirmacao.md:20 já estabelecia a regra 'não escrever o valor literal em arquivos' — este plano novo violou a própria convenção; padronizar todos os planos para `$SECRET`.

### C1 — Claim atômico (lock) na fila de trabalho: puxada com UPDATE...RETURNING
- **Origem**: achado C1 (CRITICO) | Evidência: src/index.ts:528-534 (filaPuxarPendentes = SELECT puro, sem claim) — CONFERE; src/index.ts:1976-1977 (filaMarcarFeito só no fim do processamento) — CONFERE; src/index.ts:1462-1484 (consumirItemA4: getMapeamento→criarCardLead sem lock, TOCTOU) — CONFERE; src/index.ts:1648-1683 (consumidor sync: re-check getMapeamento→criarCardLead, TOCTOU) — CONFERE. Contexto extra confirmado: schema src/index.ts:486-498 NÃO tem coluna locked_at nem status 'processing'; 2 caminhos de dreno concorrentes = /debug src/index.ts:3911 e cron a cada minuto src/index.ts:4182.
- **Problema**: filaPuxarPendentes (528-534) é um SELECT puro e filaMarcarFeito só grava no fim (1976-1977); dois drenos concorrentes (ex.: cron do minuto + /debug, ou fan-out futuro) puxam o MESMO item e ambos passam pelo TOCTOU de criar card (1462-1484 / 1648-1683) → cards duplicados e, na Função 2, WhatsApp duplicado. Dispara sempre que houver 2+ invocações de consumirFila sobrepostas no tempo com itens pendentes em comum.
- **Critérios de aceite**:
  - [ ] Puxada atômica single-statement: filaPuxarPendentes é substituída por um claim que executa UM único statement D1 `UPDATE fila_trabalho SET status='processing', locked_at=?, tentativas=tentativas+1 WHERE id IN (SELECT id FROM fila_trabalho WHERE status='pendente' AND tentativas<? ORDER BY (CASE WHEN grupo='B' THEN 0 WHEN tipo='ORC' THEN 2 ELSE 1 END), id LIMIT ?) RETURNING *` — verificável: grep no src confirma 0 SELECTs sem UPDATE puxando pendentes, e o retorno do claim já vem com status='processing'. A ordem de prioridade (B→A/F2→ORC, depois id) fica byte-idêntica à atual.
  - [ ] Concorrência disjunta (núcleo): com `wrangler dev` + D1 local semeado com 10 pendentes, 2 requisições PARALELAS ao dreno (cap=6 cada) → soma de itens processados = 10 (nunca 11+); a interseção dos ids retornados pelos 2 drenos é vazia (`SELECT id,COUNT(*) FROM fila_trabalho GROUP BY id HAVING COUNT(*)>1` sobre a união = 0 linhas); ao final `SELECT COUNT(*) FROM fila_trabalho WHERE status='feito'` = 10 e nenhum id foi marcado feito 2x.
  - [ ] Contagem de tentativas exata: após 1 tentativa que falha em 1 item, `SELECT tentativas` desse item = valor_anterior+1 (não +2) — ou seja, o incremento do claim e o de filaMarcarErro são reconciliados para exatamente 1 por tentativa; o teto FILA_MAX_TENTATIVAS (=4) continua respeitado.
  - [ ] Reclaim de travados + regressão: um item deixado em status='processing' com locked_at < now-TTL volta a ser reivindicável (seed 1 processing com locked_at antigo → próximo claim o retorna, com tentativas<FILA_MAX_TENTATIVAS; item nunca fica preso para sempre); e as bordas seguem funcionando: filaStats agrupa incluindo 'processing', `/debug?...&clear=1` (DELETE) e o cron `consumirFila(...,10,40)` rodam sem erro; runSelftestStress local continua PASSOU.
- **Plano de teste**:
  - (a) repro: Código ATUAL, tudo em D1 LOCAL. `npx wrangler dev` (D1 --local). Semear 10 pendentes benignos: `npx wrangler d1 execute kommo-cnn-db --local --command "INSERT INTO fila_trabalho (chave,tipo,paciente_id_cnn,status,tentativas,criado_em,atualizado_em) SELECT 'k'||x,'A4','28146949','pendente',0,0,0 FROM (WITH RECURSIVE c(x) AS (SELECT 1 UNION SELECT x+1 FROM c WHERE x<10) SELECT x FROM c)"`. Disparar 2 drenos em paralelo: `curl 'http://127.0.0.1:8787/debug?...&cap=6&dry=1' & curl 'http://127.0.0.1:8787/debug?...&cap=6&dry=1' & wait`. Observar em out.consumidor.itens que os MESMOS ids aparecem nos dois responses (interseção não-vazia) e soma de processados > 10 → prova o TOCTOU de leitura. Damage-free: dry=1 e itens apontam só p/ TESTE Bruno (pac 28146949).
  - (b) prova: Aplicar o claim atômico (UPDATE...RETURNING) e o reclaim por TTL. Repetir o seed de 10 pendentes e os 2 curls paralelos ao dreno (cap=6). Asserções binárias via D1 local: (1) união dos ids dos 2 responses tem interseção vazia; (2) `SELECT COUNT(*) FROM fila_trabalho WHERE status='feito'`=10; (3) soma de processados nos 2 responses = 10; (4) num item forçado a erro, tentativas subiu exatamente 1. Teste de reclaim: `INSERT ... status='processing', locked_at=0`; chamar claim → retorna esse item, tentativas<4.
  - (c) regressão: Rodar o self-test local (endpoint /selftest-stress ou runSelftestStress) → PASSOU; verificar que a ordem de bucket (B→A/F2→ORC→id) não mudou comparando o ORDER BY do claim com o de bucketPrioridadeSim (src/index.ts:2918-2925); rodar `/debug?...&clear=1` (DELETE fila) sem erro; executar 1 dreno normal `consumirFila(...,10,40)` local sobre fila mista (A4/A3/F2/ORC) e conferir contadores (criados/vinculados/movidos/adiados) idênticos ao baseline pré-mudança; confirmar filaStats retorna bucket 'processing'=0 ao fim do dreno.
- **Dependências**: nenhuma
- **Risco do deploy**: Adiciona coluna locked_at (ALTER TABLE idempotente em ensureSchema) e novo estado 'processing'. Risco principal: item preso em 'processing' se o worker morrer no meio e o reclaim por TTL estiver mal calibrado — mitigar com TTL curto (ex.: 5 min) e reclaim embutido no próprio claim. Deploy durante dreno ativo: itens que estavam em 'processing' no instante do flip são recuperados automaticamente após o TTL. Sem impacto em CNN (só-leitura preservado). Raio de dano do teste: 100% D1 LOCAL (--local, nunca --remote), sem Kommo/CNN/WhatsApp de produção; qualquer lookup residual é GET sandbox restrito a TESTE Bruno (pac 28146949 / tel 11946800329).
- **Rollback**: Reverter para filaPuxarPendentes (SELECT puro) e remover uso do status 'processing' redeployando o bundle anterior; destravar itens presos com `UPDATE fila_trabalho SET status='pendente' WHERE status='processing'`. A coluna locked_at pode permanecer (inócua, não referenciada pelo código antigo). Nenhuma perda de dados: itens em 'processing' voltam a 'pendente' e são reprocessados no próximo dreno.
- **Esforço**: M | **Status**: testado-local (2026-07-02) — deploy pendente. Fix extra: release da cauda reivindicada no break de budget (`filaAdiar`). Snapshot `scratchpad/index.after-C1.ts`.
- **Notas**: C1 é FUNDACIONAL — não depende de nenhum ID para ser implementado (dependencias vazio). Coordenação de merge (não bloqueio): A5 também mexe na fila (fila_trabalho/helpers) e A4/A6 nos wrappers Kommo chamados dentro dos consumidores; recomenda-se que C1 entre ANTES de A5 para A5 já construir sobre o claim atômico. Ponto de projeto crítico para o implementer: reconciliar o incremento de tentativas — hoje filaMarcarErro faz tentativas=tentativas+1 (src/index.ts:542); se o claim também incrementa, vira +2 por tentativa. Decidir por UMA fonte de verdade (ex.: claim incrementa; filaMarcarErro NÃO). O claim atômico (statement único UPDATE...WHERE id IN (SELECT...) RETURNING) é a forma correta em D1, pois D1/SQLite serializa e executa cada statement atomicamente — SELECT-depois-UPDATE em dois statements NÃO é seguro entre invocações. Também revisar o TOCTOU de card (1462-1484 / 1648-1683): o claim resolve a duplicação por-item da fila, mas a proteção final contra card duplicado continua sendo o UNIQUE de mapeamento/agenda_sync (getMapeamento/upsertMapeamento) — manter.

### A4 — Retry/backoff em 429/503/rede nos wrappers CNN e Kommo (parar de dead-letterar por rate-limit transitório)
- **Origem**: achado A4 (ALTO) | Evidência: src/index.ts:175 (cnnGet), :185 (cnnPost), :195 (cnnPut), :241 (kommoGet), :256 (kommoPatch), :268 (kommoPost), :277 (kommoDelete) — todos fazem `throw` direto em `!res.ok`, sem distinguir 429/503/502/504/erro de rede de erro permanente. Único retry/backoff do código é `cnnPacienteNome` (:444-450), com `catch` cego (não lê status HTTP nem header Retry-After). Na fila: `filaMarcarErro` (:540-543) incrementa `tentativas` em QUALQUER erro; `FILA_MAX_TENTATIVAS=4` (:508) → 4 falhas viram `status='erro'` (dead-letter). Catch do consumidor em `consumirFila` :1989-1993.
- **Problema**: Rate-limit/instabilidade transitória (429/503/502/504 ou fetch que lança por rede) do CNN ou Kommo vira falha DEFINITIVA do item: cada `throw` consome 1 das 4 tentativas via filaMarcarErro e, após 4, o item vai a `status='erro'` (dead-letter), perdendo uma ação legítima. Dispara em picos: dias cheios de agenda (CNN rate-limita /paciente/{id}), drenos concorrentes e Kommo perto do teto de 7 req/s. O header `Retry-After` é ignorado.
- **Critérios de aceite**:
  - [ ] Existe endpoint de auto-teste local (ex.: GET /debug-retry-selftest, atras de discoverAuthOk) que exercita o helper de retry com um fetch INJETADO/mock, sem tocar CNN/Kommo reais, e retorna JSON {attempts, waited_ms, final_status, threw}. Comando: curl local a wrangler dev --local responde 200 com esse JSON.
  - [ ] Retry-After respeitado: sequencia simulada [429 com 'Retry-After: 1', depois 200] resolve com sucesso — attempts==2, threw==false, e 1000 <= waited_ms < 2000 (esperou o valor do header, nao um backoff fixo diferente). Sequencia [503 sem Retry-After, 200] tambem resolve (attempts==2) usando backoff exponencial default.
  - [ ] Classificacao transitorio vs permanente: 429/503/502/504 e fetch-throw sao retriados ate o teto de politica (ex. 4) e, se excederem, lancam erro cuja marca/mensagem identifica 'transitorio'; ja 4xx permanente (404/400/401/403) NAO e retriado — attempts==1 e lanca imediatamente (verificavel pelo JSON do selftest para cada codigo).
  - [ ] Fila nao gasta tentativa em transitorio: quando um item da fila falha com erro transitorio, filaMarcarErro NAO incrementa `tentativas` e mantem `status='pendente'`. Prova por D1: apos 1 dreno com 1 falha transitoria simulada, `SELECT tentativas,status FROM fila_trabalho WHERE id=?` retorna tentativas==0 e status='pendente'; com erro permanente (404) retorna tentativas==1 (comportamento atual preservado).
- **Plano de teste**:
  - (a) repro: HEAD atual, wrangler dev --local + D1 local. Suba um mock HTTP em 127.0.0.1 que devolve 429 (Retry-After: 1) nas 4 primeiras chamadas e 200 depois; aponte a base do CNN para o mock via [vars] local. Enfileire 1 item de teste (TESTE Bruno, pac 28146949) e rode `curl 'http://127.0.0.1:8787/debug-tick?dry=0&cap=1&env=sandbox'` 4x. `SELECT tentativas,status,ultimo_erro FROM fila_trabalho WHERE id=?` mostra tentativas=4 e status='erro' — acao legitima virou dead-letter mesmo com o upstream so instavel.
  - (b) prova: Com a correcao: (b1) `curl .../debug-retry-selftest` prova criterios 2 e 3 (Retry-After respeitado, transitorio vs permanente) 100% local, fetch injetado, zero API externa. (b2) Repetindo o cenario do repro: os 4 drenos NAO incrementam tentativas (fica 'pendente'); quando o mock passa a 200, o dreno seguinte marca 'feito'. `SELECT COUNT(*) FROM fila_trabalho WHERE status='erro'` = 0 para o item.
  - (c) regressão: (r1) Erro permanente 404 no mock ainda dead-letterA apos 4 tentativas (tentativas sobe normal) — nao mascara falha real. (r2) Dreno feliz sem falhas: contadores processados/feito iguais ao baseline (rode /debug-tick antes e depois). (r3) Throttle Kommo intacto: 2 drenos simultaneos via /debug-tick medidos no wrangler tail somam <=~7 req/s e 0 respostas 429. (r4) Budget: com budget baixo, o backoff nao estoura o teto de 50 fetch/invocacao (subreq_total no JSON do /debug-tick < 50).
- **Dependências**: nenhuma
- **Risco do deploy**: Baixo-medio. Backoff adiciona latencia DENTRO da invocacao do Worker; se mal calibrado pode estourar wall-clock/CPU ou o teto de 50 subrequests — mitigado por teto de retries + checar orcamentoOk(budget)/bumpSubreq a cada tentativa. Alterar filaMarcarErro toca caminho compartilhado por todos os tipos da fila (A3/A4/F2/ORC); manter o incremento normal para erro permanente evita regressao. Sem migracao de schema.
- **Rollback**: Reverter o commit: wrappers voltam a `throw` direto e filaMarcarErro volta a incrementar sempre. Sem schema novo e sem estado persistente novo → rollback e so redeploy do bundle anterior (wrangler rollback / deploy do artefato anterior). Itens que porventura ja foram a 'erro' por transitorio podem ser represcados manualmente: `UPDATE fila_trabalho SET status='pendente', tentativas=0 WHERE status='erro'`.
- **Esforço**: M | **Status**: testado-local (2026-07-02) — deploy pendente. 11/11 no `/debug-retry-selftest` + prova D1 critério 4. Snapshot `scratchpad/index.after-A4.ts`. (Depende de C1: usa `filaAdiar`.)
- **Notas**: Correcao (ponto de partida, refinar): extrair helper `fetchWithRetry(doFetch, {maxRetries, budget})` que classifica 429/503/502/504/fetch-throw como transitorio, respeita `Retry-After` (segundos OU HTTP-date; senao backoff exponencial com jitter), conta bumpSubreq e aborta se !orcamentoOk(budget); aplicar aos 7 wrappers (cnnGet/cnnPost/cnnPut, kommoGet/kommoPatch/kommoPost/kommoDelete). Rede de seguranca na fila: em erro transitorio, filaMarcarErro mantem 'pendente' sem incrementar tentativas (marcar o erro com flag/classe para consumirFila distinguir). ACOPLAMENTOS (coordenacao, NAO pre-requisitos bloqueantes — por isso dependencias vazio): (1) A6 tambem edita os wrappers Kommo (kommoGet/Patch/Post) → fazer A4 e A6 no mesmo PR ou A4 primeiro para evitar conflito; (2) A5/C1 tocam a fila (consumirFila/filaMarcarErro) → preferir absorver o transitorio DENTRO do wrapper (retry antes do throw) para minimizar mudanca na fila, deixando a nao-incrementacao so como backstop; (3) B1 e o budget de subrequests → alinhar politica de retries/backoff com o teto para nao estourar 50 fetch/invocacao (cada retry chama bumpSubreq + checa orcamentoOk). RAIO DE DANO: testes rodam em wrangler dev --local + D1 local + mock em 127.0.0.1 → zero chamada externa. Se algum passo tocar sandbox por engano, so registro TESTE Bruno (pac 28146949, tel 11946800329) e afetado; chave CNN _PRODUCTION permanece so-leitura (§7.8). Se crescer para G, quebrar em A4a (helper fetchWithRetry + Retry-After + endpoint /debug-retry-selftest aplicados aos wrappers) e A4b (classificacao transitorio→nao consome tentativa em filaMarcarErro/consumirFila).

### A6 — Coordenar throttle do Kommo entre invocações (token bucket durável + backoff 429)
- **Origem**: achado A6 (ALTO) | Evidência: src/index.ts:226 (`let kommoNextSlot = 0;` — var global de módulo, portanto por-isolate), 227-234 (`kommoThrottle` espaça ~150ms lendo/escrevendo apenas `kommoNextSlot` do isolate atual), 235-279 (`kommoGet`/`kommoPatch`/`kommoPost`/`kommoDelete` chamam `kommoThrottle`+`bumpSubreq` e lançam em `!res.ok` — SEM retry/backoff em 429/503). Concorrência real: `scheduled()` roda a cada minuto (4157-4187) e `/debug-tick` (3889-3916) chamam `consumirFila` (1964) → wrappers Kommo; ticks sobrepostos ou isolates paralelos NÃO compartilham `kommoNextSlot`. CLAUDE.md confirma o gotcha ("Kommo rate limit ≈ 7 req/s por conta; kommoThrottle só coordena dentro de um isolate" e "429/503 não têm retry/backoff"). Linhas reconferidas em 2026-07-02.
- **Problema**: O espaçamento de ~150ms (~6,6 req/s) vive em `kommoNextSlot`, uma variável global de MÓDULO, logo só coordena dentro de um isolate. Quando há 2+ invocações concorrentes (tick da cron sobrepondo o anterior, cron + `/debug-tick` manual, ou isolates paralelos da Cloudflare), cada uma parte de `kommoNextSlot=0` e dispara ~6,6 req/s → soma > 7 req/s → estoura o rate-limit da conta Kommo e, como os wrappers não têm retry/backoff, o 429 vira erro do item (consome tentativa) ou derruba o tick. Bloqueia a habilitação de fan-out/sharding (PAUSADO em 2026-07-02).
- **Critérios de aceite**:
  - [ ] COORDENAÇÃO INTER-INVOCAÇÃO: com o coordenador durável ativo, 2 requisições HTTP concorrentes a `/debug-throttle-burst?n=20` em `wrangler dev` + D1 local (--persist-to), o SELECT dos timestamps de slot reservados no D1 mostra que, ordenados, quaisquer 2 reservas consecutivas distam ≥150ms E nenhuma janela deslizante de 1000ms contém >7 reservas (portanto ≤7 req/s combinado).
  - [ ] SEM SLOT DUPLICADO (reserva atômica): no mesmo teste, `SELECT COUNT(*) = COUNT(DISTINCT slot_ts) FROM throttle_slots` retorna verdadeiro (cada slot emitido no máx. 1×; 40 reservas concorrentes = 40 slots distintos, 0 corrida).
  - [ ] BACKOFF 429/503: com mock que responde 429 c/ `Retry-After: 1` nas 2 primeiras chamadas e 200 na 3ª, cada wrapper (`kommoGet`/`kommoPatch`/`kommoPost`/`kommoDelete`) retorna o corpo do 200 sem lançar, após no máx. 3 tentativas, respeitando `Retry-After` (verificável por selftest in-code que conta tentativas e afere o atraso ≥1000ms).
  - [ ] REGRESSÃO SINGLE-ISOLATE: `/debug-selftest?mode=logic|fuzz|stress` continua PASS (INV1–INV3b) e 1 invocação isolada de `/debug-tick?...&dry=1` reporta `subreq_total ≤ budget` e taxa ≤7 req/s no `wrangler tail` (o caminho de 1 isolate não regride em vazão nem em contagem de subrequests).
- **Plano de teste**:
  - (a) repro: (a) Provar que o modelo por-isolate fura sob concorrência. Como `wrangler dev` local geralmente atende requisições concorrentes no MESMO isolate (não reproduz naturalmente), reproduzir por modelagem: adicionar selftest temporário (mesmo padrão de runSelftestStress ~2962) que instancia DOIS estados independentes de `nextSlot` (cada um começando em 0) e emite 20 reservas cada com o algoritmo atual de kommoThrottle; asserir que a taxa combinada em alguma janela de 1s é >7 (esperado ~13,2 req/s) → FALHA, evidenciando o bug. Alternativa de campo (só observação, sem escrita): `wrangler tail` durante um tick longo sobreposto ao próximo mostra rajada >7 req/s a `*.kommo.com` e respostas 429.
  - (b) prova: (b) Implementado o coordenador durável (token bucket em D1: tabela `throttle_slots`/contador com reserva atômica via UPDATE ... RETURNING ou INSERT idempotente) + retry/backoff 429/503 nos wrappers: rodar `wrangler dev` + D1 local e disparar 2× `/debug-throttle-burst?n=20` em paralelo; conferir AC1 (espaçamento ≥150ms e ≤7/janela-1s via SELECT) e AC2 (slots distintos). Rodar o selftest de backoff (AC3) com mock 429→200. Todos verdes.
  - (c) regressão: (c) `/debug-selftest?mode=logic&fuzz&stress` PASS; `/debug-tick?env=sandbox&dry=1&cap=10&budget=40` drena o lote com `subreq_total ≤ budget` e sem erros; confirmar que o caminho de 1 isolate mantém ~6,6 req/s (não fica mais lento por I/O extra ao D1 a ponto de estourar o wall-clock do tick de 1 min). Kill-switch (ex.: `THROTTLE_DURABLE=0`) volta ao espaçamento in-isolate sem quebrar os wrappers.
- **Dependências**: A4
- **Risco do deploy**: Alto acoplamento de superfície: o coordenador toca TODOS os caminhos de escrita/leitura Kommo (4 wrappers). Bug de over-throttle trava todas as escritas (backlog cresce); bug de under-throttle mantém a rajada 429. O bucket durável adiciona 1+ ida ao D1 por chamada Kommo (latência/carga) — atenção ao wall-clock do tick de 1 min e ao fato de D1 não contar subrequest mas contar tempo. Mitigar com feature-flag e entregar a v1 estática antes da durável.
- **Rollback**: Kill-switch por env var (ex.: `THROTTLE_DURABLE=0` + redeploy) faz cair de volta ao espaçamento in-isolate (`kommoNextSlot`), sem migração. A tabela `throttle_slots` é aditiva (CREATE IF NOT EXISTS) → seu descarte é seguro. Rollback total do Worker: `wrangler rollback`/deploy do bundle anterior (`b0167bfa`).
- **Esforço**: G | **Status**: backlog
- **Notas**: Quebra sugerida (G → 3 subtarefas): A6.1 (P) retry/backoff exponencial + honra `Retry-After` em 429/503 nos 4 wrappers — COINCIDE com A4 (mesmos wrappers); se A4 já entregar isso, A6 reusa e AC3 vira regressão de A4. A6.2 (M) budget/teto estático por invocação (v1 sem concorrência real) — ACOPLA com B1 (contador `subreqUsados` global também sofre corrida sob ticks sobrepostos; corrigir junto). A6.3 (G) token bucket DURÁVEL cross-isolate em D1 (ou Durable Object com estado single-threaded, que resolve o problema de forma mais limpa que D1) com reserva atômica de slot + backoff — habilita fan-out (hoje PAUSADO). Acoplamentos a coordenar no merge: A4 (wrappers Kommo), A5/C1 (mesma camada D1/fila, `filaPuxarPendentes` sem lock ~528), B1 (orçamento de subrequests). Raio de dano dos testes: `/debug-throttle-burst` e o dry-run devem usar MOCK de fetch ou, no máx., GET de leitura no lead do TESTE Bruno (CNN 28146949 / tel 11946800329); dry=1 → `consumirItemOrcamento` só faz `kommoGet` de leitura (nenhum move/patch); CNN produção intocada (só-leitura §7.8). Dependência declarada como soft: A6.3 (desenho do bucket durável) pode começar em paralelo; a coordenação com A4 é para não duplicar/colidir o backoff nos wrappers.

### A5 — Dead-letter observável + requeue controlado para itens 'erro' da fila_trabalho
- **Origem**: achado A5 (ALTO) | Evidência: CONFERE. src/index.ts:540-543 (filaMarcarErro: novoStatus='erro' quando (tentativasAtuais+1) >= FILA_MAX_TENTATIVAS, com FILA_MAX_TENTATIVAS=4 em src/index.ts:508). src/index.ts:530-532 (filaPuxarPendentes: SELECT ... WHERE status='pendente' AND tentativas < 4 — item já 'erro' NUNCA mais é puxado pelo dreno). filaStats (src/index.ts:545-550) expõe apenas a CONTAGEM agregada por status; consumida em /debug-audit (src/index.ts:3874) e /debug-tick (src/index.ts:3914-3914) — não há lista dos itens em erro, nem exposição de ultimo_erro, nem alerta, nem endpoint de requeue. O scheduled tick (src/index.ts:4181-4184) dreia a fila todo minuto mas nunca inspeciona/alerta o count de 'erro'. Único reset hoje é /debug-tick?clear=1 (src/index.ts:3903), que DELETA a fila inteira (destrutivo, não é requeue seletivo). Nenhuma rota /debug-fila-erros ou /debug-fila-requeue existe (grep vazio).
- **Problema**: Item que falha 4x vira status='erro' terminal e some do dreno (filaPuxarPendentes filtra tentativas<4), sem lista, sem ultimo_erro exposto, sem alerta e sem requeue. Dispara sempre que uma ação de sync (criar card A4, mover A3/F2/ORC) falha 4 drenos seguidos — a ação é perdida silenciosamente e não há caminho não-destrutivo de recuperação (só o clear=1, que apaga tudo).
- **Critérios de aceite**:
  - [ ] Observabilidade — lista: GET /debug-fila-erros (auth discoverAuthOk) retorna JSON com array de itens status='erro', cada elemento com {id,tipo,chave,paciente_id_cnn,agenda_id_cnn,grupo,tentativas,ultimo_erro,atualizado_em} e um campo total. Com 1 linha 'erro' semeada no D1 local (ultimo_erro='boom'), a resposta tem total=1 e itens[0].ultimo_erro=='boom' (verificar via curl+jq). Sem auth → HTTP 401.
  - [ ] Requeue seletivo dry por padrão: GET /debug-fila-requeue?id=<X> (sem dry=0) NÃO altera o D1 — SELECT status,tentativas FROM fila_trabalho WHERE id=<X> continua ('erro',4) — e o JSON retorna dry:true e would_requeue:1.
  - [ ] Requeue commit: GET /debug-fila-requeue?id=<X>&dry=0 retorna requeued:1 e o D1 confirma status='pendente', tentativas=0, ultimo_erro IS NULL para id=<X>; em seguida GET /debug-tick?dry=1&... inclui id=<X> em consumidor.itens (item reentra no dreno).
  - [ ] Requeue em lote com escopo: com 3 linhas 'erro' + 2 linhas 'feito' semeadas, GET /debug-fila-requeue?all=1&dry=0 retorna requeued:3, e no D1 as 3 viram status='pendente'/tentativas=0 enquanto SELECT COUNT(*) WHERE status='feito' permanece 2 (não toca 'feito' nem 'pendente' pré-existentes).
  - [ ] Caminho de erro preservado (não-regressão): item semeado 'pendente' que falha o dreno 4x (via /debug-tick?dry=0 forçando exceção) termina status='erro' com tentativas=4 — filaMarcarErro (src/index.ts:540-543) inalterado.
- **Plano de teste**:
  - (a) repro: wrangler dev + D1 local. Semear 1 poison item: wrangler d1 execute <DB> --local --command "INSERT INTO fila_trabalho (chave,tipo,status,tentativas,ultimo_erro,criado_em,atualizado_em) VALUES ('t-erro-1','A4','erro',4,'boom',strftime('%s','now'),strftime('%s','now'))". Rodar GET /debug-tick?dry=0&prod=0 (dreno) → o item NÃO aparece em consumidor.itens (filaPuxarPendentes o exclui) e GET /debug-audit devolve fila.erro=1 mas nenhum detalhe do item: comprova o 'erro terminal mudo, sem lista nem requeue'.
  - (b) prova: Após implementar: (1) GET /debug-fila-erros?secret=... → total=1, itens[0].ultimo_erro=='boom'. (2) GET /debug-fila-requeue?id=1 (dry) → would_requeue:1 e SELECT confirma ainda ('erro',4). (3) GET /debug-fila-requeue?id=1&dry=0 → requeued:1; SELECT status,tentativas,ultimo_erro WHERE id=1 → ('pendente',0,NULL). (4) GET /debug-tick?dry=1 → id=1 presente em consumidor.itens. (5) Lote: semear 3 'erro'+2 'feito'; /debug-fila-requeue?all=1&dry=0 → requeued:3, COUNT(status='feito')==2 intacto.
  - (c) regressão: (a) /debug-tick?dry=0 continua drenando pendentes normalmente (processados>0 com item pendente válido). (b) /debug-audit continua retornando fila{} com contagens por status. (c) /debug-selftest?mode=logic (asserções in-code) passa. (d) Caminho de erro intacto: semear 1 item 'pendente' que force exceção no consumidor, drenar 4x com dry=0 → status='erro', tentativas=4 (filaMarcarErro inalterado). (e) /debug-tick?clear=1 continua funcionando.
- **Dependências**: nenhuma
- **Risco do deploy**: Baixo-médio. Duas rotas novas (uma leitura /debug-fila-erros, uma escrita em D1 /debug-fila-requeue), ambas atrás de discoverAuthOk; nenhuma escrita em CNN/Kommo no próprio endpoint. Risco principal: requeue mal-usado re-injeta item 'venenoso' que volta a falhar — mitigado por dry-run default e por tentativas voltar a 0, reentrando no ciclo normal de 4 tentativas antes de 'erro' de novo (sem loop infinito). Interação com C1 (concorrência de drenos sobre a fila): requeue apenas marca 'pendente'/tentativas=0; a segurança de dreno concorrente é escopo de C1 e independe desta mudança. Raio de dano externo dos TESTES = 0 (só D1 local; dreno mantido em dry=1). Se algum dia rodado em prod com dreno dry=0, o requeue pode disparar uma ação Kommo real — por isso semear apenas linhas sintéticas (TESTE Bruno, paciente CNN 28146949) nos ensaios.
- **Rollback**: Reverter o deploy: remover as rotas /debug-fila-erros e /debug-fila-requeue (e o warn opcional no scheduled tick). Sem migração de schema — reusa colunas existentes status/tentativas/ultimo_erro de fila_trabalho — portanto rollback é apenas re-deploy da versão anterior; nenhum dado precisa ser desfeito.
- **Esforço**: M | **Status**: testado-local (2026-07-02) — deploy pendente. Só 2 endpoints aditivos (sem warn no scheduled — YAGNI, F1 cobre alerta). crit5 (4x→erro/tentativas=4) não-regride: A5 não toca filaMarcarErro nem o caminho de erro. Snapshot `scratchpad/index.after-A5.ts`.
- **Notas**: Parte de partida (refinar na implementação, não implementar agora): /debug-fila-erros = SELECT id,tipo,chave,paciente_id_cnn,agenda_id_cnn,grupo,tentativas,ultimo_erro,atualizado_em FROM fila_trabalho WHERE status='erro' ORDER BY atualizado_em DESC LIMIT (param n, default 50) + total. /debug-fila-requeue = UPDATE fila_trabalho SET status='pendente', tentativas=0, ultimo_erro=NULL, atualizado_em=? WHERE status='erro' AND (id=?|all=1) [+filtro opcional tipo=]; dry por padrão (dry!=0 lista/preview, dry=0 commita), seguindo a convenção de /debug-corrigir (src/index.ts:3882). Auth: discoverAuthOk (Authorization header == WEBHOOK_SECRET), igual às demais /debug-*. Alerta opcional (fecha o 'sem alerta'): no scheduled tick (src/index.ts:4181), após o dreno, se filaStats(env).erro>0 emitir console.warn(`fila_erro=${n}`) — visível no wrangler tail; critério extra binário se incluído: com >=1 'erro' na fila, a linha 'fila_erro=' aparece no tail, e com 0 não aparece. Relação com C1: C1 toca concorrência/dedup do dreno da mesma fila; A5 não altera o dreno, só reintroduz itens — sem bloqueio, mas revisar juntos para evitar conflito de merge nas rotas /debug-*. Sem relação com A4/A6 (wrappers Kommo) nem B1 (budget). Evidência revista em 2026-07-02, linhas conferem exatamente.

### B2 — Adicionar lease cooperativo em D1 para serializar ticks do cron e jobs /debug
- **Origem**: achado B2 (MEDIO) | Evidência: src/index.ts:4157-4187 (scheduled sem mutex, corpo todo em ctx.waitUntil); wrangler.toml:16 (crons = ["* * * * *"]); src/index.ts:3889-3916 (/debug-tick: resetSubreq@3891, consumirFila@3911, DELETE FROM fila_trabalho em clear=1 @3903); src/index.ts:528-534 (filaPuxarPendentes = SELECT status='pendente' SEM claim atômico); src/index.ts:1976-1977 (filaMarcarFeito só APÓS o write Kommo); src/index.ts:53-56 (subreqUsados global de módulo compartilhado); tabela `cursores` @458-459 disponível para hospedar o lease.
- **Problema**: O tick do cron (1/min) e os jobs manuais /debug-tick não têm exclusão mútua. Se um tick M (produtores + dreno em waitUntil) passar de 60s, o tick M+1 começa em paralelo; e um /debug-tick manual roda junto do cron. Como filaPuxarPendentes só faz SELECT dos pendentes (sem claim atômico) e o filaMarcarFeito só ocorre DEPOIS do write no Kommo, duas execuções concorrentes puxam as MESMAS linhas → escritas duplicadas no Kommo (2 cards p/ mesmo (paciente,grupo) na janela TOCTOU, movimentação de etapa em dobro). Além disso, o global subreqUsados é resetado/incrementado pelas duas execuções no mesmo isolate → guarda de orçamento corrompida; e /debug-tick?clear=1 faz DELETE na fila enquanto o dreno do cron a processa.
- **Critérios de aceite**:
  - [ ] EXCLUSAO MUTUA: com 2 chamadas concorrentes ao corpo do tick (2x POST /debug-tick contra o mesmo D1 local), exatamente 1 adquire o lease e trabalha; a outra retorna resposta documentada 'skipped: lease em uso por <owner> até <ts>' com consumidor.processados=0 e sem bloco produtor; SELECT na tabela de lease mostra 1 única linha de owner ativo.
  - [ ] SEM DRENO DUPLO: semear exatamente 10 itens pendentes no D1 local e disparar 2 drenos simultâneos → soma de consumidor.processados nas 2 respostas = 10 (não 11+), e um SELECT confirma 0 linhas com transição pendente→feito mais de uma vez (cada uma das 10 com atualizado_em setado 1x; 0 linhas de auditoria 'feito' duplicadas).
  - [ ] TTL / ANTI-DEADLOCK: inserir manualmente linha de lease com expiração no passado (now-1s) → próximo /debug-tick ADQUIRE e processa; inserir lease com expiração no futuro (now+120s) → próximo /debug-tick RETORNA skipped e processados=0. Comprova que lease expira (não trava para sempre) e que o release ocorre em finally mesmo se o tick lançar exceção.
  - [ ] SEGURANCA DO CLEAR MANUAL: enquanto um lease está ativo (segurado por tick simulado), POST /debug-tick?clear=1 retorna skipped e SELECT COUNT(*) FROM fila_trabalho permanece inalterado (o DELETE não roda sem o lease).
- **Plano de teste**:
  - (a) repro: wrangler dev --local. ensureSchema + semear 10 pendentes NOOP na fila_trabalho local (INSERT direto via script D1 local). No código ATUAL (sem lease), disparar 2x POST /debug-tick?env=sandbox&dry=0 em paralelo (curl & curl & wait) usando um tipo de item que não chama API externa (ou stub local do wrapper Kommo). Observar: soma de consumidor.processados > 10 e/ou SELECT mostrando linhas marcadas feito 2x; e ambas as respostas rodaram produtores (dupla intenção de escrita). Alvo=sandbox, dry quando possível → 0 efeito externo.
  - (b) prova: Com o lease implementado (adquirir/soltar em try/finally em scheduled() e em /debug-tick, respeitado por AMBOS), repetir o disparo concorrente idêntico: exatamente 1 resposta trabalha (processados somando 10), a outra retorna 'skipped: lease'. SELECT confirma as 10 linhas marcadas feito exatamente 1x (0 duplicata). Repetir os casos de TTL (lease passado→adquire; futuro→skip) e o caso clear=1 sob lease ativo (fila inalterada). Adicionar 1 asserção no /debug-selftest (modo logic) cobrindo aquire→skip→expire.
  - (c) regressão: 1) /debug-tick sequencial (não concorrente) executado 2x seguidas: ambos ADQUIREM (lease liberado entre eles) e processam normalmente. 2) Um tick único (caminho scheduled via /debug-tick) ainda roda produtores no gatilho de minuto (min%10==0 sync; ORC; véspera nos horários) — lógica de horário intacta. 3) Forçar exceção dentro do tick e confirmar que o lease é liberado no finally (próximo tick adquire, não fica travado até o TTL). 4) /debug-selftest?mode=logic continua verde.
- **Dependências**: nenhuma
- **Risco do deploy**: Baixo-médio. Lease mal calibrado pode travar todos os ticks até o TTL se o release falhar — mitigado por release em finally + TTL curto (ex.: 5 min) que auto-expira. TTL curto demais reintroduz a sobreposição; TTL longo demais atrasa o próximo tick após um crash. Kill-switch: se o lease bloquear tudo, apagar a linha de lease no D1 (DELETE FROM cursores WHERE nome='tick_lease') ou reverter o bundle.
- **Rollback**: Reverter o deploy para o bundle anterior (sem lease) — comportamento volta ao atual (sem exclusão mútua). Alternativa sem redeploy: DELETE da linha de lease no D1 remoto libera qualquer trava residual imediatamente. Nenhuma migração destrutiva (lease reusa a tabela `cursores` existente ou tabela nova IF NOT EXISTS).
- **Esforço**: M | **Status**: testado-local (2026-07-02) — deploy pendente. Reusa `cursores.tick_lease` (sem migração). CAS SQL + 4 critérios via endpoint. Snapshot `scratchpad/index.after-B2.ts`.
- **Notas**: Abordagem escolhida = lease COARSE (1 lock por tick) conforme a correção sugerida, hospedado na tabela `cursores` (nome='tick_lease', valor=JSON {owner, expira_em}) — sem mudança de schema obrigatória. Aquire = UPDATE condicional atômico em D1 (UPDATE cursores SET valor=... WHERE nome='tick_lease' AND (valor IS NULL OU expira_em<now); checar meta.changes==1 para vencer a corrida; INSERT OR IGNORE inicial). RELACIONADOS (não bloqueantes): (1) B1/budget — o lease elimina a corrida no global subreqUsados (src/index.ts:53-56); se B1 tornar o budget per-invocation, os dois se reforçam mas nenhum depende do outro. (2) A5/C1/fila — alternativa mais fina seria claim atômico por item em filaPuxarPendentes (UPDATE ... SET status='processando' RETURNING) que tocaria a fila; deixei como alternativa, não como dependência, pois o lease coarse já cobre B2. (3) A4/A6/wrappers Kommo — só entram se o teste de repro precisar stub do Kommo; preferir tipo NOOP local para manter o teste sem API externa. RAIO DE DANO: testes em wrangler --local + D1 local, target=sandbox, dry/NOOP → 0 escrita real no Kommo, 0 escrita no CNN (prod é só-leitura §7.8). Se algum passo exigir toque vivo, usar apenas TESTE Bruno (paciente CNN 28146949) em sandbox.

### B1 — Tornar o orçamento de subrequests local por invocação (remover contador módulo-global)
- **Origem**: achado B1 (MEDIO) | Evidência: src/index.ts:53-56 (let subreqUsados=0 + resetSubreq/bumpSubreq/orcamentoOk) e 4159 (resetSubreq() como 1ª linha do ctx.waitUntil do scheduled) — CONFEREM, sem deslocamento de linha. Vetores adicionais de corrida no mesmo isolate: os /debug-* do fetch handler também chamam resetSubreq() (ex.: 3880, 3891, 3920, 3305, 3379, 3461, 3508, 3535, 3564, 3656, 3742) e leem subreqUsados (3886, 3910-3912). bumpSubreq() está dentro dos 7 wrappers (cnnGet/Post/Put 173/180/190; kommoGet/Patch/Post/Delete 237/249/261/273); orcamentoOk() em ~20 sítios (206, 364, 680, 1131, 1266, 1837, 1864, 1969, 2636, 2678/2696, 3422/3440, 3476, 3519, 3573, 3664, 3751).
- **Problema**: O orçamento de subrequests é um contador módulo-global (subreqUsados) com reset no início de cada entrada. Como scheduled e os /debug-* rodam no mesmo isolate, invocações sobrepostas (waitUntil do minuto N ainda drenando quando o minuto N+1 dispara, ou um /debug-tick concorrente) compartilham/zeram o contador: dispara quando um resetSubreq() de uma invocação zera o orçamento de outra em andamento (→ estoura o teto real de 50 fetch/invocação com erro "Too many subrequests") ou quando os bumps somados de duas invocações fazem cada uma parar cedo (orcamentoOk falso), deixando itens pendentes.
- **Critérios de aceite**:
  - [ ] grep em src/index.ts retorna 0 ocorrências de 'let subreqUsados', 'function resetSubreq', 'function bumpSubreq' e de 'orcamentoOk()' sem argumento-objeto; o orçamento passa a ser criado 1x por invocação (um newBudget(...) em scheduled e um em cada handler /debug-*) e trafega por parâmetro pelos wrappers e produtores/consumidores. `npx tsc --noEmit` passa.
  - [ ] Repro de sobreposição (wrangler dev + D1 local + mock lento): 2 requisições /debug-tick concorrentes contra o mock; ANTES da correção, pelo menos uma resposta traz subreq_total menor que os hits que o mock atribuiu àquela invocação (contador zerado pela outra) OU o mock registra >50 hits de um mesmo isolate sem o freio local disparar. DEPOIS da correção: o subreq_total de cada resposta é igual ao nº de hits do mock atribuíveis àquela invocação e nenhuma invocação individual excede seu max; `wrangler tail` mostra 0 erros 'Too many subrequests'.
  - [ ] Não-starve: 2 drenos /debug-tick?dry=0 simultâneos sobre 10 itens pendentes em fila_trabalho (D1 local) → soma de processados = 10 (nenhum item fica pendente por budget compartilhado falsamente esgotado) e nenhuma invocação para com parou_orcamento/adiados por causa dos bumps da outra.
  - [ ] Regressão: 1 /debug-tick isolado contra mock que devolve mais páginas que o budget → mesma fronteira de antes (parou_orcamento:true ou adiados>0) e subreq_total ≤ budget passado; um disparo do scheduled (wrangler dev + trigger /__scheduled) completa sem erro e drena a fila como antes.
- **Plano de teste**:
  - (a) repro: Subir wrangler dev com D1 local (wrangler dev --local). Apontar CNN_BASE/kommoBase para um mock HTTP local (workerd ou node http) que responde ~300ms e conta hits por invocação (via header/query que o wrapper injeta). Semear 10 itens pendentes em fila_trabalho. Disparar 2x /debug-tick?dry=0&env=sandbox concorrentes (curl & curl &). Observar em wrangler tail e nas respostas: subreq_total inconsistente (menor que os hits do mock daquela invocação) e/ou um mesmo isolate acumulando >50 hits sem o freio local — evidencia global compartilhado/zerado.
  - (b) prova: Aplicar B1 (budget local por objeto/parametro). Repetir os 2 /debug-tick concorrentes: cada resposta traz subreq_total == hits do mock daquela invocação; nenhuma invocação excede seu max; SELECT no D1 (SELECT status,COUNT(*) FROM fila_trabalho GROUP BY status) confirma 10 processados e 0 item processado 2x; wrangler tail sem 'Too many subrequests'.
  - (c) regressão: 1 /debug-tick isolado contra mock com muitas páginas → parou_orcamento/adiados na mesma fronteira e subreq_total ≤ budget. Disparar o scheduled local (wrangler dev, POST /__scheduled ou --test-scheduled) e confirmar tick completo sem erro + fila drenada. `npx tsc --noEmit` verde. Raio de dano: testes usam mock local ou target=sandbox; zero escrita no CNN de produção (§7.8, só GET) e zero WhatsApp/lead vivo; se algo tocar Kommo, usar só o lead de TESTE Bruno.
- **Dependências**: A4, A6, A5, C1
- **Risco do deploy**: Medio. Altera a assinatura dos 7 wrappers Kommo/CNN e de ~15 funções produtoras/consumidoras (threading do objeto budget) — erro de threading pode passar budget errado e sub/super-fetch. Sem mudança de schema D1. Mitigar com tsc, o teste de concorrência e um tick canário em sandbox antes do flip; comportamento em regime normal (invocação única) fica idêntico.
- **Rollback**: Reverter o commit (arquivo único src/index.ts) e redeploy do bundle anterior (wrangler rollback ou wrangler deploy da tag prévia). Sem migração D1 para desfazer. Kill-switch existente (ORC_ENABLED) permanece válido para conter o produtor de orçamento se necessário.
- **Esforço**: G | **Status**: backlog
- **Notas**: Correção: substituir subreqUsados/resetSubreq/bumpSubreq/orcamentoOk por um objeto de estado local (ex.: const budget = newBudget(50); com budget.bump() e budget.ok(max)) criado 1x por invocação em scheduled e em cada /debug-*, trafegado por parâmetro até os wrappers (cnnGet/Post/Put, kommoGet/Patch/Post/Delete). Já existe precedente de budget LOCAL na simulação (src/index.ts:2997-2999, subreqTick/BUDGET) — usar o mesmo padrão em produção. Dependências: A4/A6 tocam os wrappers Kommo e A5/C1 tocam a fila/dreno (consumirFila usa orcamentoOk); recomendo que A4/A6/A5/C1 aterrissem ANTES para B1 fazer o threading sobre as assinaturas já finais e evitar rebase duplo — se forem paralelizados, coordenar a mudança de assinatura dos wrappers em um único PR. Quebra do G em 2: B1a — introduzir o tipo Budget, threading nos 7 wrappers + no caminho quente do scheduled (produtores do tick + consumirFila), mantendo shim (wrapper aceita budget opcional; sem ele cai no global) para não quebrar compilação; B1b — threading nos /debug-* e sweeps/backfill/corrigirCards restantes, remover o global e o shim, e adicionar o teste de regressão de concorrência.

### A1 — Não reconciliar órfãos A quando a paginação do produtorSync foi parcial (e auto-curar card em Perdido com agenda vigente)
- **Origem**: achado A1 (ALTO) | Evidência: src/index.ts:1503-1509 (while `pag < totalPag` sem guarda de incompletude e sem `&& orcamentoOk()`; linha 1506 `catch { break; }` silencioso deixa `todas` parcial); 1555-1556 (gate `mudou`→`sem_mudanca` retorna sem re-empurrar = não auto-cura); 1578-1595 (reconciliação de órfãos A que enfileira orphan→Perdido); 1586 (única guarda é `if (!aAgs.length) continue;`); 1588 (destino cai em STAGE_CANCELADA_PERDIDO=143). Confirmado em 2026-07-02: números de linha batem exatamente.
- **Problema**: Se uma página da paginação do `produtorSync` falha (429/estouro de subrequests), o `catch { break; }` (1506) encerra o loop sem sinalizar e `todas` fica parcial. A agenda A FUTURA vigente de um paciente pode ficar numa página não lida → `comVigenteA` não inclui o pid → a reconciliação de órfãos (1578-1595) enfileira um item orphan que move o card, de forma DURÁVEL, para Cancelada–Perdido (143). Pior: o gate `mudou` (1555-1556) impede a auto-cura no ciclo seguinte, pois `agenda_sync` já tem o mesmo status/ts da vigente → nunca re-empurra o card de volta.
- **Critérios de aceite**:
  - [ ] AC1 (skip órfãos em ciclo parcial): quando `produtorSync` sai do while com `pag < totalPag` (fetch de página lançou/abortou), o objeto retornado tem `paginacao_incompleta === true`, `out.orfaos_a === 0`, e `SELECT COUNT(*) FROM fila WHERE tipo='A3' AND payload LIKE '%"kind":"orphan"%'` no D1 local NÃO cresce (0 orphans novos) nesse run.
  - [ ] AC2 (repro A/B, pid TESTE Bruno 28146949): com mock CNN devolvendo totalPaginas=2, página 0=OK contendo apenas uma agenda A TERMINAL (CANCELADO) do pid e página 1 lançando erro — build ATUAL enfileira 1 orphan p/ o pid e (no consumo) move o lead para status_id=143; build CORRIGIDO enfileira 0 orphans p/ o pid e o `status_id` do lead permanece inalterado. Verificável por `SELECT` na fila + leitura da etapa do lead (mock/sandbox).
  - [ ] AC3 (regressão do caso legítimo): em run com paginação COMPLETA (`pag >= totalPag`, sem erro) o objeto tem `paginacao_incompleta === false` e um órfão A genuíno (lead mapeado, só agenda A TERMINAL na janela, sem futura) continua gerando exatamente 1 item orphan: `SELECT COUNT(*) ... kind=orphan AND paciente_id_cnn=<pid> = 1`; contagens `out.sync_a`/`out.sync_b` do caminho feliz inalteradas vs. baseline pré-fix.
  - [ ] AC4 (auto-cura): dado lead A mapeado atualmente em status_id=143 e uma agenda A FUTURA vigente presente em janela COMPLETA, `produtorSync` enfileira 1 item sync p/ (pid,'A') MESMO com `agenda_sync` inalterado (o gate `mudou` deixa de suprimir quando o card está em etapa terminal apesar de agenda vigente); após o consumo `status_id !== 143`. Verificável: 1 sync na fila + etapa do lead pós-consumo.
- **Plano de teste**:
  - (a) repro: wrangler dev + D1 local. Seed: `mapeamento_lead` com (paciente_id_cnn=28146949, grupo='A', lead_id=<lead sandbox de teste>) e `agenda_sync` com status/ts de uma agenda A. Subir um mock do CNN (ou apontar CNN_BASE p/ um endpoint local) que responda `/agenda/lista`: totalPaginas=2, página 0 = 200 com uma agenda A CANCELADO (terminal) do pid 28146949, página 1 = HTTP 429/erro. Disparar o produtor via /debug (job=sync, dry=1). Observar: `out.agendas` só conta a página 0; `SELECT * FROM fila WHERE paciente_id_cnn=28146949 AND payload LIKE '%orphan%'` retorna 1 linha com destino=143 → bug (a A futura vigente do pid estava na página 1 não lida).
  - (b) prova: Com o fix: o mesmo run devolve `paginacao_incompleta=true`, `out.orfaos_a=0` e a query de orphans acima retorna 0 linhas p/ o pid (AC1/AC2 lado B). Depois trocar o mock p/ página 1 = 200 com a agenda A FUTURA vigente e rodar novamente (paginação completa): 0 orphans, 1 item sync p/ (pid,'A'); consumir a fila (dry=0 em sandbox) e confirmar `status_id` do lead != 143 (AC4). Rodar /selftest (sim N3/decidir*) e confirmar verde.
  - (c) regressão: Run com paginação COMPLETA e um órfão A genuíno (lead mapeado + só agenda A TERMINAL, sem futura): confirmar `paginacao_incompleta=false`, exatamente 1 orphan enfileirado e movido p/ 143 como hoje (AC3); e que um paciente com A/B vigente continua gerando os mesmos `sync_a`/`sync_b` do baseline pré-fix. Rodar os selftests existentes (/selftest, N3 stress) sem novas falhas.
- **Dependências**: nenhuma
- **Risco do deploy**: Baixo-médio. O núcleo (sinalizar `paginacao_incompleta` + pular órfãos + guarda `orcamentoOk` no while) é conservador: na dúvida NÃO rebaixa cards, então não introduz movimentos novos indevidos — no pior caso adia a reconciliação de um órfão legítimo por 1 ciclo (o cron re-roda). A parte de auto-cura (AC4) altera o gate de enfileiramento e pode re-empurrar cards; deve ir para sandbox primeiro. Sem migração de schema. Idempotente (chaves de fila iguais → `ja_na_fila`).
- **Rollback**: Reverter o commit (sem migração de D1 a desfazer). Se um card de teste for movido em sandbox durante a validação, reverter via /debug-move e restaurar agenda com set-cnn-status. Nenhum efeito em CNN prod (read-only) nem em WhatsApp.
- **Esforço**: G | **Status**: backlog
- **Notas**: Raio de dano: teste usa mock CNN local + registro TESTE Bruno (pid 28146949) em pipeline sandbox; CNN prod só-leitura (§7.8), sem WhatsApp, sem mover lead vivo. Quebrar o G em 2: A1a (P-M, prioridade/independente) = sinalizar `paginacao_incompleta` (detectar `pag < totalPag` na saída OU flag no catch), pular a reconciliação de órfãos 1578-1595 nesse ciclo, e adicionar `&& orcamentoOk(budget)` ao while 1503 para parada limpa dentro do orçamento. A1b (M) = auto-cura: quando o card está em etapa terminal (143) apesar de agenda A vigente, forçar re-enfileiramento do sync contornando o gate `mudou` (1555-1556). Coordenação (não são pré-requisitos rígidos, mas sequenciar merges): A1b lê a etapa do card via kommoGet → casar com A4/A6 (robustez dos wrappers Kommo) e escreve na fila → casar com A5/C1 (semântica da fila); o guard `orcamentoOk` do while pode conflitar com B1 se B1 renomear/alterar o orçamento — rebasear A1a sobre B1 se B1 for primeiro. Aplicar o mesmo padrão de `paginacao_incompleta`/guarda aos outros loops de paginação com `catch { break; }` que alimentam decisões terminais (ex.: 1102, 1220, 1332, 1736, 2012) fica fora do escopo de A1 mas vale registrar como follow-up.

### A2 — Guard de colisão de telefone ao vincular: não colapsar 2 pacientes CNN num lead
- **Origem**: achado A2 (ALTO) | Evidência: src/index.ts:1384-1408 `acharLeadPorTelefone` casa lead por `phoneKey` (DDD+8) filtrado por pipeline — só LÊ, não decide dono. O colapso last-write-wins está nos 3 chamadores: (1) consumidor de fila `consumirItemA4` 1467-1476 → `escreverVinculoCnn` (330 sobrescreve o campo "ID Paciente CNN" do card) + `upsertMapeamento` (1473) grava (Y,grupo)→lead; (2) backfill legado 1270-1303; (3) consumer sync `consumirItemA3` ~1658-1663. Tabela `mapeamento` (463-466) tem PK (paciente,grupo) e SEM UNIQUE em `lead_id_kommo` → 2 pacientes→1 lead é permitido. `getFieldValue` (289-292) lê o dono do card a partir dos leads já buscados. Existe remediação pós-fato (`splitColisaoTelefone` 3459 / `/debug-split-colisao`; `consolidarColisao` 3507) mas NENHUM guard preventivo. CLAUDE.md documenta o gotcha ("2 pacientes CNN com mesmo telefone colapsam num lead — 4 casos reais") e o invariante violado "1 paciente CNN = 1 lead". Confere.
- **Problema**: Ao vincular por telefone, se o card achado já pertence a OUTRO paciente CNN (campo "ID Paciente CNN" preenchido com pid diferente, ou já existe linha `mapeamento` desse lead com outro paciente), o código vincula assim mesmo: sobrescreve o "ID Paciente CNN" do card com o pid atual e grava um 2º mapeamento apontando pro mesmo lead. Resultado: 2 pacientes (familiares c/ mesmo número) disputam 1 card e as agendas de ambos se atropelam (last-write-wins). Dispara sempre que 2 pacientes CNN distintos compartilham o mesmo phoneKey no mesmo grupo (A ou B) — 4 casos vivos hoje.
- **Critérios de aceite**:
  - [ ] COLISÃO BLOQUEADA: dado lead L cujo campo 'ID Paciente CNN' = pac X, ao processar A4 do pac Y!=X (mesmo phoneKey, mesmo grupo) e acharLeadPorTelefone retornar L, o worker NÃO chama escreverVinculoCnn nem upsertMapeamento ligando Y->L. Verificação: log do mock Kommo tem 0 PATCH /leads/L que troque 'ID Paciente CNN' para Y; e `SELECT COUNT(*) FROM mapeamento WHERE lead_id_kommo='L'` continua = 1 (só X).
  - [ ] ROTA ALTERNATIVA: no caso de colisão, Y é roteado pra card distinto — apos o dreno, `SELECT lead_id_kommo FROM mapeamento WHERE paciente_id_cnn='Y' AND grupo='A'` é NULL ou !=L; se card novo foi criado, o mock registra exatamente 1 POST /leads/complex para Y (novo lead id != L). E `SELECT COUNT(DISTINCT paciente_id_cnn) FROM mapeamento WHERE lead_id_kommo='L'` = 1.
  - [ ] SEM FALSO-POSITIVO: quando o lead achado L tem 'ID Paciente CNN' vazio OU igual a Y, a vinculação procede como hoje — resultado r='vinculado', `SELECT lead_id_kommo FROM mapeamento WHERE paciente_id_cnn='Y' AND grupo='A'`=L, e o mock registra o PATCH normal de escreverVinculoCnn. (Prova de que o guard não quebra o caminho legítimo.)
  - [ ] SEM CUSTO EXTRA DE SUBREQ: processar 1 item A4 colidente consome o MESMO nº de GETs Kommo do código atual (2 GETs: /contacts + /leads batelado; 0 GET adicional por item) — o dono é lido de getFieldValue sobre os leads já buscados. Medição: subreqUsados/log do mock no dreno de 1 item colidente <= baseline atual.
- **Plano de teste**:
  - (a) repro: wrangler dev --local (D1 local isolado) + mock Kommo local (servidor http em 127.0.0.1 apontado por override KOMMO_BASE — hook mínimo de testabilidade, sub-tarefa) que responde: GET /contacts?query=...&with=leads -> 1 contato com PHONE casando e lead L=999 embarcado; GET /leads?filter[id][]=999 -> lead 999 pipeline_id=PIPELINE_CAPTACAO e custom 'ID Paciente CNN'='27426411' (dono X); PATCH/POST só gravados no log. Passos: (1) seed D1 `INSERT INTO mapeamento(paciente_id_cnn,grupo,lead_id_kommo,telefone_norm,criado_em,atualizado_em) VALUES('27426411','A','999','5192478008',0,0)`; (2) enfileira item A4 do pac 27448477 (Y), grupo A, mesmo telefone; (3) dreno dry=0 via endpoint de tick/dreno. Observado no BUILD ATUAL (sem guard): mock registra PATCH /leads/999 setando 'ID Paciente CNN'=27448477 (dono sobrescrito) e `SELECT COUNT(*) FROM mapeamento WHERE lead_id_kommo='999'`=2 -> bug reproduzido.
  - (b) prova: Repetir os mesmos 3 passos no BUILD com guard. Esperado (binário): (a) 0 PATCH em /leads/999 trocando 'ID Paciente CNN' para 27448477 no log do mock; (b) `SELECT COUNT(*) FROM mapeamento WHERE lead_id_kommo='999'`=1 (só 27426411); (c) `SELECT lead_id_kommo FROM mapeamento WHERE paciente_id_cnn='27448477' AND grupo='A'` NULL ou != '999'; (d) resultado do consumidor = 'criado' (novo card, 1 POST /leads/complex p/ Y) ou 'colisao_detectada' (pulado/enfileirado p/ split), nunca 'vinculado'->999.
  - (c) regressão: (1) Caminho legítimo: lead 999 com 'ID Paciente CNN' vazio -> dreno do item Y -> r='vinculado', mapeamento(Y,A)=999, 1 PATCH escreverVinculoCnn no mock (guard não gera falso-positivo). (2) Idempotência/anti-ressurreição: re-drenar o mesmo item -> 'ja_mapeado', 0 novo PATCH/POST. (3) Ferramentas de colisão existentes intactas: `/debug-split-colisao?dry=1` e `/debug-consolidar-colisao?dry=1` respondem sem erro sobre o D1 seedado. (4) Type-check: `npx wrangler deploy --dry-run` (ou tsc) passa. Raio de dano: TODAS as chamadas Kommo batem no mock local (KOMMO_BASE override) -> ZERO escrita no Kommo/WhatsApp reais; CNN não é tocado (nome do paciente stubado ou GET read-only do TESTE Bruno 28146949). Se, por exceção, rodar contra Kommo real, restringir ao lead do TESTE Bruno.
- **Dependências**: nenhuma
- **Risco do deploy**: Baixo-médio. Só adiciona checagem antes de vincular; não altera schema. Risco de FALSO-POSITIVO: se 'ID Paciente CNN' do card estiver vazio/errado no legado, o guard pode achar que não é colisão (ok) ou, se comparar mal, deixar de vincular caso legítimo — mitigado pelo critério 3 (só bloqueia quando dono != vazio E != pid atual). Toca o consumidor de fila (consumirItemA4) e 2 call-sites — coordenar merge com achados da fila (A5/C1) e budget (B1) pra não estourar o teto de 50 subreq. Sem fan-out (fila sem lock permanece pausada).
- **Rollback**: Reverter o commit do guard (redeploy do bundle anterior — rollback `b0167bfa` per CLAUDE.md). Guard é puramente aditivo e sem migração de dados, então revert restaura o comportamento atual sem limpeza de estado. Colisões já materializadas continuam tratáveis por `/debug-split-colisao` e `/debug-consolidar-colisao` independentemente.
- **Esforço**: M | **Status**: backlog
- **Notas**: A correção deve cobrir os 3 pontos de vinculação-por-telefone, não só a fila: consumirItemA4 (1467-1476), backfill legado (1270-1303) e consumer sync consumirItemA3 (~1658-1663). Refino sugerido de acharLeadPorTelefone: além de leadId/nome/contatos, retornar donoPid = getFieldValue(doGrupo, fields['ID Paciente CNN']) — sem GET extra, pois doGrupo já vem de leadsFull. Decisão de política a bater no design (não implementar agora): em colisão, (a) criar card novo pra Y via criarCardLead (consistente com splitColisaoTelefone), ou (b) pular e deixar o split pós-fato assumir. Preferência: (a) preventivo, mas confirmar com a Clarissa por causa do volume de duplicatas (124 cards A). Coordenação (não bloqueio): A5/C1 tocam o mesmo consumidor de fila; B1 é o budget de subreq — mesclar por cima. Pré-req de teste: hook KOMMO_BASE override (kommoBase 221-222 hoje hardcoda .kommo.com) pra apontar ao mock local; é sub-tarefa P dentro deste item.

### A3 — Fila A3: impedir dedup permanente de reversão de status (purgar 'feito' ou rotacionar chave)
- **Origem**: achado A3 (ALTO) | Evidência: src/index.ts:1560 (chave `A3:sync:${pid}:${grupo}:${vig.id}:${vig.status}:${tsBucket}` — sem componente rotativo, permanente); :519 (`INSERT OR IGNORE INTO fila_trabalho` dedup pela coluna `chave` UNIQUE, def. em :488); :536 (`filaMarcarFeito` só faz `UPDATE ... status='feito'` — a linha persiste para sempre); :3903 (única purga é manual: `?clear=1` → `DELETE FROM fila_trabalho`). Suporte: :1591 (chave `A3:orphan:${pid}:A:${destino}` — mesma classe, ainda mais exposta, sem status/ts); :1555-1556 (guarda `mudou` via agenda_sync detecta a reversão, mas a fila engole o item). Todas as linhas conferem na leitura de 01/07.
- **Problema**: A coluna `chave` da fila é UNIQUE e a linha nunca é removida ('feito' persiste; só `?clear=1` apaga tudo). Quando um par (status,horário) já processado se repete — reversão confirma→desconfirma→confirma sem remarcar (tsBucket constante), ou órfão Perdido→reativa→Perdido — o `INSERT OR IGNORE` bate na linha antiga e descarta o novo item silenciosamente; `agenda_sync` detecta a mudança (mudou=true) mas a fila engole o trabalho e o card fica preso na etapa anterior. Dispara sempre que o mesmo agendamento oscila de volta a um status já aplicado mantendo o mesmo minuto de início.
- **Critérios de aceite**:
  - [ ] AC1 (reversão não some): dado agenda_sync.last_cnn_status='AGENDADO' e uma linha 'feito' com chave `A3:sync:P:A:AG1:CONFIRMADO:<bucket>` já existente, ao (re)enfileirar essa MESMA chave (produtor com mudou=true), `SELECT COUNT(*) FROM fila_trabalho WHERE chave='A3:sync:P:A:AG1:CONFIRMADO:<bucket>' AND status='pendente'` retorna 1 (antes da correção retorna 0). Equivalente no produtor: out.enfileirados=1 e out.ja_na_fila=0 para esse item na 3ª rodada.
  - [ ] AC2 (coalescing em voo preservado): após `DELETE FROM fila_trabalho`, dois enfileiramentos consecutivos da MESMA chave nova sem dreno entre eles → `SELECT COUNT(*) FROM fila_trabalho WHERE chave=<chave>` = 1 (não 2). A dedup contra itens ainda pendentes continua funcionando.
  - [ ] AC3 (sem âncora obsoleta / crescimento ilimitado): após um dreno completo, nenhuma linha 'feito' antiga permanece como bloqueio — `SELECT COUNT(*) FROM fila_trabalho WHERE status='feito'` = 0 (design delete-on-feito) OU toda linha 'feito' tem atualizado_em dentro da janela de retenção escolhida (design TTL-purge). Escolha binária: uma das duas condições vale.
  - [ ] AC4 (órfão coberto): mesma prova do AC1 aplicada à chave `A3:orphan:P:A:143` (Perdido→reativa→Perdido) → novo item 'pendente' após reincidência (COUNT=1).
- **Plano de teste**:
  - (a) repro: Local, wrangler dev + D1 local, SEM tocar CNN (defeito é na camada da fila). (1) `wrangler d1 execute DB --local --command "DELETE FROM fila_trabalho"`. (2) simula 1º CONFIRMADO já processado: `INSERT OR IGNORE INTO fila_trabalho(chave,tipo,status,tentativas,criado_em,atualizado_em) VALUES('A3:sync:P:A:AG1:CONFIRMADO:100','A3','pendente',0,0,0)` e depois `UPDATE fila_trabalho SET status='feito' WHERE chave='A3:sync:P:A:AG1:CONFIRMADO:100'`. (3) reversão: re-enfileira a MESMA chave `INSERT OR IGNORE ... VALUES('A3:sync:P:A:AG1:CONFIRMADO:100',...,'pendente',0,1,1)`. (4) `SELECT COUNT(*) n FROM fila_trabalho WHERE chave='A3:sync:P:A:AG1:CONFIRMADO:100' AND status='pendente'` → n=0 (bug: reversão perdida). Repetir com chave `A3:orphan:P:A:143` para o caso órfão.
  - (b) prova: Aplicar a correção (delete-on-feito em filaMarcarFeito, OU DELETE da linha 'feito' de mesma chave antes do INSERT, OU componente de geração rotativa na chave em :1560/:1591) e repetir os passos do repro: no passo (4) `n=1` (AC1/AC4). End-to-end opcional com produtor real: em wrangler dev, forçar est≠status e chamar /debug-tick?job=sync&prod=1&dry=1 → out.enfileirados=1 / out.ja_na_fila=0 para a chave reincidente.
  - (c) regressão: (AC2) `DELETE FROM fila_trabalho`; dois `INSERT OR IGNORE` da mesma chave NOVA em 'pendente' sem dreno entre eles → `SELECT COUNT(*) WHERE chave=<chave>`=1. (AC3) rodar /debug-tick?dry=1 drenando um lote misto e conferir `SELECT status,COUNT(*) FROM fila_trabalho GROUP BY status` — sem acúmulo indevido de 'feito' conforme design. Entorno: /debug-audit responde com bloco `fila` sem erro; ordenação de filaPuxarPendentes (B antes de A, ORC por último) inalterada num dreno de itens B+A+ORC; jobs vespera/orcamento drenam normalmente.
- **Dependências**: nenhuma
- **Risco do deploy**: Médio: mexe no núcleo compartilhado da fila (geração de chave e/ou marcar 'feito'), afetando TODOS os jobs (sync/orphan/vespera/orcamento) que usam fila_trabalho. delete-on-feito remove o histórico de itens processados na tabela (auditoria em `auditoria` permanece). É PONTO DE COORDENAÇÃO com A5/C1, que também tocam a fila — se ambos reescreverem filaEnfileirarLote/filaMarcarFeito haverá conflito; sequenciar ou mesclar. Sem migração destrutiva se a mudança for só lógica; coluna nova (ex.: geração) deve ser aditiva (CREATE TABLE IF NOT EXISTS já existente + ALTER aditivo). Nenhuma escrita no CNN (defeito é interno D1).
- **Rollback**: Reverter o commit — mudança isolada em filaEnfileirarLote (:510-526), filaMarcarFeito (:535-538) e geração das chaves em :1560/:1591. Sem migração de schema irreversível; se coluna 'geração' for adicionada, é aditiva (rollback = redeploy do bundle anterior, coluna órfã inofensiva). Nenhum dado de paciente/lead alterado. Se necessário, `?clear=1` limpa a fila e o próximo produtor reconstrói a partir de agenda_sync.
- **Esforço**: M | **Status**: backlog
- **Notas**: A correção deve cobrir AMBAS as chaves A3 (`A3:sync` em :1560 e `A3:orphan` em :1591) — a órfã é ainda mais exposta (sem status/ts na chave). Recomendo delete-on-feito (ou DELETE da linha 'feito' de mesma chave antes do INSERT) como caminho primário: transforma a chave UNIQUE em coalescing de itens EM VOO (intenção real) e mantém agenda_sync/orcamento_sync como a verdadeira idempotência 'move só 1x por mudança'. Rotação por dia é mitigação fraca (falha se a reversão repetir no mesmo dia); rotação por par from->to (est.status->vig.status) também falha em ciclos ≥2. Observabilidade do bug hoje: produtor conta sync_a/sync_b++ (:1557) mas out.enfileirados (:1600, delta de pendentes) não sobe e out.ja_na_fila (:1601) incrementa — sintoma diagnosticável. COORDENAR com A5/C1 (mesma tabela) antes de implementar.

### A7 — Tornar recuperação de criação de card resiliente a duplicata (lookup por ID Paciente CNN)
- **Origem**: achado A7 (MEDIO) | Evidência: src/index.ts:1436 (criarCardLead: kommoPost "/leads/complex" cria o card ANTES de upsertMapeamento (1447) + upsertAgendaSync (1448), sem transação/atomicidade; leadId extraído em 1445; o card já carrega ID Paciente CNN em 1441). Recuperação no retry só por telefone: consumirItemA4:1467 (acharLeadPorTelefone, sem guard) e consumirItemA3:1657-1683 (guard telefone.length>=8 em 1657 PULA o lookup se telefone curto/vazio → cai direto em criarCardLead 1677). acharLeadPorTelefone definido em 1384 (busca /contacts?query=<8 dígitos>; nunca consulta por ID Paciente CNN).
- **Problema**: Em criarCardLead o POST /leads/complex cria o card e SÓ DEPOIS grava o D1, sem transação; se o POST sucede mas o upsert D1 falha, o item volta a pendente e é redrenado, e a recuperação (só por telefone) recria o card → duplicata no Kommo quando telefone é vazio/curto ou o índice de contatos da Kommo ainda não refletiu o contato recém-criado.
- **Critérios de aceite**:
  - [ ] REPRO (prova do bug no código atual): com harness que faz upsertMapeamento LANÇAR na 1ª tentativa logo após POST bem-sucedido (mock Kommo devolve lead id 12345), drenar o MESMO item de fila (tipo A4, pid=28146949, grupo A, payload telefone="") duas vezes resulta em EXATAMENTE 2 chamadas POST /leads/complex no log do mock e 2 cards com o mesmo ID Paciente CNN=28146949.
  - [ ] FIX (create-once): com o mesmo harness/injeção de falha, drenar o mesmo item 2× após a correção → EXATAMENTE 1 POST /leads/complex para (pid,grupo); a 2ª drenagem resolve via lookup por ID Paciente CNN e retorna r:"vinculado" (ou ja_mapeado); SELECT COUNT(*) FROM mapeamento WHERE paciente_id_cnn='28146949' AND grupo='A' = 1 apontando lead_id=12345.
  - [ ] FIX (telefone vazio): item com telefone="" e mock já com 1 card carregando ID Paciente CNN=28146949 na pipeline do grupo A, SEM mapeamento no D1 → 1 drenagem → 0 POST /leads/complex e r:"vinculado" (recuperação deixa de depender do telefone).
  - [ ] FILTRO por pipeline: o novo helper acharLeadPorPacienteCnn(pid, pipelineId) devolve o lead cujo ID Paciente CNN==pid E pipeline_id==pipelineId, e devolve undefined quando o único match está em OUTRA pipeline (mesmo critério de acharLeadPorTelefone); verificável com mock retornando 2 leads (um por pipeline).
  - [ ] REGRESSÃO (sem custo no caminho feliz): criação limpa (sem falha injetada, sem card pré-existente) → 1 POST, r:"criado", mapeamento gravado, e a CONTAGEM de GETs ao mock é idêntica ao baseline pré-fix (o lookup novo só dispara no ramo de recuperação, após getMapeamento E acharLeadPorTelefone falharem).
- **Plano de teste**:
  - (a) repro: wrangler dev com D1 LOCAL (wrangler dev --local) e Kommo apontada a um mock HTTP local (via override de base URL de teste; kommoBase hoje fixa https://${KOMMO_SUBDOMAIN}.kommo.com — expor um KOMMO_BASE opcional só-para-teste é o enabler; alternativa: teste unitário Miniflare/vitest chamando consumirItemA4 com env mockado). Passos: (1) ensureSchema no D1 local; (2) enfileirar 1 item tipo A4 (pid=28146949 TESTE Bruno, grupo A, payload telefone=""); (3) injetar falha: fazer o INSERT em mapeamento lançar na 1ª tentativa (ex.: renomear a tabela mapeamento antes da 1ª drenagem, ou env.DB stub que lança 1×); (4) POST no endpoint /debug consumidor 2×. Esperado (bug): log do mock = 2 POST /leads/complex; mock guarda 2 cards com ID Paciente CNN=28146949. Raio de dano: mock Kommo local apenas — ZERO escrita em Kommo/CNN/WhatsApp reais; CNN de prod não é tocada (só leitura).
  - (b) prova: Mesmo harness com a correção aplicada (novo helper acharLeadPorPacienteCnn + chamada no ramo de recuperação de consumirItemA4 e consumirItemA3, antes de criarCardLead). (a) Drenar 2× com a falha D1 na 1ª tentativa → assert: 1 POST /leads/complex no log do mock; na 2ª drenagem há 1 GET /leads filtrando por ID Paciente CNN; resultado r:"vinculado"; contagem de cards do mock para pid=28146949 = 1; SELECT COUNT(*) FROM mapeamento p/ (28146949,'A') = 1 com lead_id=12345. (b) Caso telefone vazio (AC3): pré-semear mock com 1 card ID Paciente CNN=28146949 na pipeline A, sem mapeamento D1, telefone="" → 1 drenagem → 0 POST, r:"vinculado".
  - (c) regressão: (1) Caminho feliz: drenar 1 item novo sem falha e sem card pré-existente → 1 POST, r:"criado", mapeamento+agenda_sync gravados, e nº de GETs ao mock == baseline (lookup novo NÃO dispara). (2) Duplicata A+B: cenário consumirItemA3 com temOutro=true e sem card → card A criado com sufixo " (duplicata)" no nome (inalterado). (3) acharLeadPorTelefone continua devolvendo o lead da pipeline do grupo quando o mesmo telefone tem lead em Captação e Pós-Venda (filtro por pipeline preservado).
- **Dependências**: A4, A6
- **Risco do deploy**: Baixo. Sem migração D1/schema. Adiciona no máximo 1 GET Kommo e só no ramo raro de recuperação (getMapeamento miss + acharLeadPorTelefone miss), então caminho feliz não muda carga nem latência. Risco residual: o índice de custom field da Kommo também pode ter lag no retry imediato (mesma classe do lag de /contacts) → em janela muito curta ainda poderia duplicar; mitiga a esmagadora maioria dos casos (telefone vazio/curto = 100% coberto). A sintaxe exata do filtro Kommo v4 por custom field (filter[custom_fields_values]/query) precisa ser confirmada na implementação — hoje não há uso desse filtro no código.
- **Rollback**: Reverter os edits de código (novo helper acharLeadPorPacienteCnn + 2 call-sites em consumirItemA4/consumirItemA3). Sem estado persistido novo, sem migração → rollback restaura a recuperação só-por-telefone sem efeitos colaterais. Se o filtro por custom field vier a gerar erro/latência na Kommo, o helper pode ser envolvido em try/catch que degrada para o comportamento atual (ignora e segue).
- **Esforço**: M | **Status**: backlog
- **Notas**: Correção sugerida (refinar): criar helper acharLeadPorPacienteCnn(pid, pipelineId, env) espelhando acharLeadPorTelefone (busca leads por ID Paciente CNN e filtra por pipeline_id) e chamá-lo no ramo de recuperação ANTES de criarCardLead em consumirItemA4 (após 1467) e consumirItemA3 (após 1658/antes de 1672); se achar, gravar mapeamento e retornar \"vinculado\". Coordenar com A5/C1 (mexem em consumirFila/consumirItem*, mesmos call-sites) para evitar conflito de merge — por isso as deps A4/A6 (wrappers Kommo, onde o novo GET se apoia; se A4 adicionar retry ao kommoPost, atenção ao vetor extra de duplicata por POST reenviado após timeout). Enabler de teste: expor um override de base URL Kommo só-para-teste (env.KOMMO_BASE) para poder apontar wrangler dev a um mock local; hoje kommoBase (index.ts:221) é fixa. Considerar também gravar um marcador write-ahead no D1 antes do POST como reforço futuro (fora do escopo M deste item).

### A8 — Corrigir recência do reflexo de orçamento que rebaixa venda real do pós-venda
- **Origem**: achado A8 (MEDIO) | Evidência: src/index.ts:820-822 (orcamentosRecentes filtra recência SÓ p/ APROVADO; ORC_APROVACAO_MAX_DIAS=60 na L819); src/index.ts:805-812 (decidirEtapaOrcamento: some(APROVADO) e "recente" por MAX id rodam sobre o conjunto JÁ filtrado); consumidor L1922-1924/1957 (moveLeadToStage); gate L40-43 (ETAPAS_ORC_PODE_AGIR inclui STAGE_POS_TRATAMENTO_INICIADO → porta ABERTA p/ tirar do pós-venda). Live ref: paciente 20531441 via GET /debug-orcamento?env=production&paciente=20531441&decidir=1.
- **Problema**: Um APROVADO com dataAprovacao >60d é descartado por orcamentosRecentes; se o paciente tem um orçamento não-aprovado mais novo (ex.: CANCELADO/PERDIDO com id maior), decidirEtapaOrcamento não vê o APROVADO e classifica pelo mais-recente → Venda Perdida/Em Análise. Dispara quando o consumidor ORC reavalia esse paciente (produtor enfileira por criação nova/sweep) e a etapa atual está em ETAPAS_ORC_PODE_AGIR (inclui Tratamento Iniciado): o lead de uma venda real é movido de Pós-Venda para Pós-Consulta/Venda Perdida.
- **Critérios de aceite**:
  - [ ] PENDÊNCIA HUMANA resolvida: existe doc em D:/clarissa-bergmann/kommo-cnn/docs/ com data e a REGRA escolhida entre R1=aplicar a mesma recência a CANCELADO/PERDIDO, R2='recente' pelo MAX id do conjunto COMPLETO mantendo APROVADO como precedência, ou R3=APROVADO nunca expira p/ efeito de rebaixar do pós-venda (verificável: arquivo nomeia uma das regras).
  - [ ] Novo caso no N1 (runSelftestLogic) que HOJE falharia: decidirEtapaOrcamento(orcamentosRecentes([{id:1,status:'APROVADO',dataAprovacao: hoje-90d},{id:2,status:'CANCELADO'}], cutoff=hoje-60d), false). Pré-correção retorna {PIPELINE_POS_CONSULTA, STAGE_POSCONS_VENDA_PERDIDA}; pós-correção retorna o alvo da regra escolhida; `wrangler dev --local` + GET /debug-selftest?mode=logic → failed=0.
  - [ ] Regressão: os 11 casos atuais de decidirEtapaOrcamento + gates ETAPAS_ORC_PODE_AGIR (L2735-2777) e o fuzz continuam passando — GET /debug-selftest?mode=logic e ?mode=fuzz retornam failed=0.
  - [ ] Live read-only (só GET, nenhum move): GET /debug-orcamento?env=production&paciente=20531441&decidir=1 retorna 'decisao' coerente com a regra (não Venda Perdida/Em Análise quando existe um APROVADO real no array 'orcamentos'); wrangler tail confirma 0 chamadas de escrita (PATCH/POST) ao Kommo durante a chamada.
- **Plano de teste**:
  - (a) repro: Local determinístico: em `wrangler dev --local` adicionar ao N1 o caso 'APROVADO(id=1, dataAprovacao=hoje-90d) + CANCELADO(id=2)' passando por orcamentosRecentes(cutoff=hoje-60d) → decidirEtapaOrcamento(...,false); GET /debug-selftest?mode=logic mostra este caso com obtido={PIPELINE_POS_CONSULTA,STAGE_POSCONS_VENDA_PERDIDA} (bug). Confirmação ao vivo READ-ONLY: GET /debug-orcamento?env=production&paciente=20531441&decidir=1 → 'decisao' classifica como Venda Perdida/Em Análise embora 'orcamentos' contenha um APROVADO. Raio de dano: apenas GET no CNN prod (§7.8 só-leitura) + leitura D1 (temAgendaFutura); nenhum kommoGet de escrita, nenhum lead movido.
  - (b) prova: Após implementar a regra escolhida (ajuste em orcamentosRecentes e/ou decidirEtapaOrcamento — funções puras), o novo caso do N1 passa e GET /debug-selftest?mode=logic → failed=0; re-rodar GET /debug-orcamento?env=production&paciente=20531441&decidir=1 e conferir que 'decisao' não rebaixa o paciente com APROVADO real. Verificar no consumidor com DB local: enfileirar item ORC de um paciente-mock (via /debug com registro TESTE Bruno 28146949) cujo estado replique APROVADO antigo + CANCELADO novo e drenar em dryRun=1 → resultado NÃO é 'moveu p/ Venda Perdida'.
  - (c) regressão: GET /debug-selftest?mode=logic e ?mode=fuzz → failed=0 (11 casos de decisão + 12 gates + expiração + addDiasISO intactos, em especial '[ABERTO,APROVADO]→Tratamento Iniciado' e 'temFutura vence'). GET /debug-orcamento-impacto?env=production&di=2026-01-01 (read-only) rodado ANTES e DEPOIS da correção: comparar contadores (moveria_tratamento_iniciado, nao_recente, adiado_etapa) e confirmar que a mudança de classificação bate com a regra e não altera pacientes sem APROVADO.
- **Dependências**: nenhuma
- **Risco do deploy**: Médio-alto: decidirEtapaOrcamento é o núcleo do reflexo; regra errada pode, no próximo dreno do cron, mover em massa leads entre Pós-Venda e Pós-Consulta. Mitigar rodando /debug-orcamento-impacto (read-only, budget-aware) ANTES do flip pra medir quantos leads mudam de classe, e manter o cron do produtor/consumidor ORC pausado até validar a amostra.
- **Rollback**: git revert do commit (mudança isolada nas funções puras orcamentosRecentes/decidirEtapaOrcamento) + redeploy. A idempotência por orcamento_sync.ultima_etapa evita corrupção de estado no D1; leads eventualmente movidos indevidamente antes do revert são restaurados via /debug-move usando o snapshot do /debug-orcamento-impacto (de→para) capturado no pré-deploy.
- **Esforço**: P | **Status**: backlog
- **Notas**: É PENDÊNCIA HUMANA (dono do negócio): não decidir a regra aqui — só instrumentar. A correção em si é pequena (<1h) e puramente nas funções sem I/O; o gargalo é a decisão de negócio. Observação de consistência: no consumidor (L1925-1927) o 'resumo' é calculado sobre o array NÃO filtrado (orcs), enquanto a 'decisao' usa o filtrado — ao escolher a regra, alinhar resumo e decisao para não gravar orcamento_sync.ultimo_status='APROVADO' com ultima_etapa=Venda Perdida. Sem dependência lógica de A4/A6 (wrappers Kommo) nem A5/C1 (fila); se esses editarem consumirItemOrcamento no mesmo intervalo, coordenar merge (mesma função), mas não há ordem de execução obrigatória.

### E45 — Assinar webhooks (HMAC + janela timestamp/nonce) e tornar handlers idempotentes
- **Origem**: achado E45 (MEDIO) | Evidência: src/index.ts:3770-3771 (webhookAuthOk lê SÓ ?secret= da query); :4147 (gate único antes dos POST); :4150-4151 (rotas /webhook/lead-agendado→handleLeadAgendado, /webhook/confirmacao→handleConfirmacao); :867-881 (handleLeadAgendado — guard de idempotência só sequencial em :880-881, corre em concorrência; cnnPost sem target = sandbox por default :178); :943-961 (handleConfirmacao — SEM guard de idempotência: cnnPut :956 + moveLeadToStage :959 a cada POST; cnnPut sem target = sandbox :188). Confere.
- **Problema**: Qualquer POST que conheça o secret (que trafega na URL — logável em proxy/histórico/tail) injeta ou reexecuta W1/W2: move leads REAIS no Kommo e grava agenda no CNN sandbox. Não há HMAC do corpo, nem janela timestamp/nonce anti-replay, e handleConfirmacao não é idempotente. Dispara quando: (a) o secret vaza, ou (b) o Kommo/Salesbot reenvia o mesmo webhook (retry/duplo-clique) → replay re-dispara cnnPut+moveLeadToStage; ou (c) 2 POSTs concorrentes de lead-agendado antes da 1ª gravar ID Agenda → duplo /agenda/novo.
- **Critérios de aceite**:
  - [ ] Idempotência confirmacao (replay): 2 POST idênticos a /webhook/confirmacao (mesmo corpo/leadId) → no wrangler tail EXATAMENTE 1 cnnPut(/agenda/alteracao-status) e 1 moveLeadToStage; a 2ª resposta é HTTP 200 {skipped:true,reason:'replay'} com 0 fetch de saída; SELECT COUNT(*) FROM webhook_dedup WHERE chave='confirmacao:<lead>:<agenda>' = 1.
  - [ ] Idempotência lead-agendado concorrente: 2 POST concorrentes a /webhook/lead-agendado para o mesmo lead novo (sem ID Agenda) → EXATAMENTE 1 cnnPost(/agenda/novo) no tail (não 2) e 1 linha em webhook_dedup; a 2ª resposta é skipped.
  - [ ] Anti-replay temporal: POST cujo timestamp assinado está fora do TTL (ex.: > 300s no passado) → HTTP 401/409 e 0 fetch de saída; o mesmo corpo assinado dentro da janela → HTTP 200 e efeito ocorre 1×.
  - [ ] Ligação ao corpo + secret fora da URL: corpo adulterado (leadId trocado) reutilizando assinatura antiga → HTTP 401; grep de searchParams.get('secret') dentro de webhookAuthOk retorna 0 ocorrências e requisição sem o header de assinatura → HTTP 401 (só passa HMAC-SHA256(rawBody, WEBHOOK_SECRET) correto no header).
- **Plano de teste**:
  - (a) repro: wrangler dev (local) + D1 local (--persist-to scratchpad). .dev.vars: WEBHOOK_SECRET=teste e KOMMO_BASE_URL apontando p/ um stub node http (em scratchpad) que LOGA cada chamada e devolve: p/ confirmacao um lead com 'ID Agenda CNN' preenchido; p/ lead-agendado um lead sem ID Agenda. Enviar 2× o MESMO POST form-encoded 'leads[status][0][id]=<lead dedicado TESTE Bruno>' para http://127.0.0.1:8787/webhook/confirmacao?secret=teste. Observar no wrangler tail 2 cnnPut + 2 moveLeadToStage (prova a NÃO-idempotência) e que ?secret=teste na URL é aceito (prova o vazamento na URL).
  - (b) prova: Após a correção, repetir os 2 POST assinados: tail mostra EXATAMENTE 1 cnnPut + 1 move, 2ª resposta skipped, SELECT no webhook_dedup=1. Replay com timestamp velho → 401. Corpo adulterado com assinatura antiga → 401. Requisição sem header de assinatura → 401. grep 'searchParams.get("secret")' em webhookAuthOk = 0. Para concorrência: disparar 2 curl paralelos a /webhook/lead-agendado do mesmo lead → 1 só cnnPost(/agenda/novo) no tail.
  - (c) regressão: 1 POST legítimo novo (assinatura válida, dentro da janela) a lead-agendado e a confirmacao → HTTP 200 e efeito ocorre 1×. Rotas /debug* (discoverAuthOk via header Authorization) e o scheduled/C1 seguem funcionando. wrangler dev sobe sem erro de schema (ensureSchema cria webhook_dedup idempotente). assertCnnWritable intacto e handlers seguem default sandbox → 0 escrita no CNN prod.
- **Dependências**: nenhuma
- **Risco do deploy**: ALTO se trocado abruptamente: exigir header/HMAC quebra os webhooks vivos do Kommo (W1/W2 param, leads deixam de mover) caso o Kommo/Salesbot não consiga enviar o header. Mitigar com transição ADITIVA atrás de flag WEBHOOK_STRICT_AUTH: aceitar ao mesmo tempo o secret-na-query legado E a nova assinatura por header; confirmar no wrangler tail que o Kommo entrega a forma nova; só então flipar para rejeitar a legada. webhook_dedup é aditiva (CREATE IF NOT EXISTS), sem migração.
- **Rollback**: Setar WEBHOOK_STRICT_AUTH=0 (reaceita ?secret= legado) ou redeploy do bundle anterior. Tabela webhook_dedup é inofensiva se deixada (não altera o fluxo legado). Sem migração de dados a desfazer.
- **Esforço**: G | **Status**: backlog
- **Notas**: RAIO DE DANO do teste: se KOMMO_BASE_URL não for o stub local, o teste moveria etapa de lead REAL no Kommo; por isso apontar Kommo p/ stub local e usar só o lead dedicado (TESTE Bruno, paciente CNN 28146949). Nesses 2 handlers cnnPost/cnnPut default = sandbox (§ index.ts:178/188) → 0 risco ao CNN prod. CORREÇÃO DA EVIDÊNCIA vs. achado: o finding diz 'alterar status CNN' — na verdade é CNN SANDBOX (handlers não passam target=production); o dano de produção real é no Kommo (moves + writes de campo). SPIKE OBRIGATÓRIO antes de codar: confirmar o que o webhook do Kommo/Salesbot consegue enviar (header custom? assinatura HMAC do corpo? nonce/timestamp?). Se o Kommo NÃO puder enviar header/HMAC, o critério 4 degrada para: manter secret mas (a) rotacionar p/ alta entropia, (b) mover p/ path não-adivinhável, (c) derivar anti-replay/idempotência 100% worker-side de campos que o Kommo já manda (leadId + last_modified). SUGESTÃO DE QUEBRA (G→2 itens): E45a = idempotência worker-side (tabela webhook_dedup + guard em handleConfirmacao + guard de concorrência em handleLeadAgendado) — sem dependência externa, 100% testável local (M); E45b = endurecimento de transporte (HMAC do corpo + janela timestamp/nonce + secret em header), gated no spike do Kommo (M/G). COORDENAÇÃO SUAVE (não-bloqueante): A4/A6 tocam os wrappers Kommo (moveLeadToStage/setLeadFields) — evitar conflito de merge; A5/C1 tocam a fila (fila_trabalho) — o padrão INSERT OR IGNORE por chave UNIQUE (index.ts:486-499) é o mesmo a reusar em webhook_dedup.

### F1 — Registrar cada tick de forma durável em D1 (status/erro/contadores) + alerta externo no catch e habilitar [observability]
- **Origem**: achado F1 (MEDIO) | Evidência: src/index.ts:4183-4184 (catch do scheduled só faz console.error, efêmero) — CONFIRMADO; wrangler.toml:1-17 sem bloco [observability] — CONFIRMADO (arquivo tem 17 linhas, nenhum [observability]); audit() definido em src/index.ts:568-577 e chamado APENAS em ramos de sucesso (1978-1980, 2068), enquanto o catch de consumirFila (1989-1993) grava filaMarcarErro mas NÃO audita — CONFIRMADO. Ponto de enganche de schema: ensureSchema em src/index.ts:454.
- **Problema**: Se o corpo do scheduled() lança à noite (cascata de 429 Kommo, CNN 5xx, D1 timeout), o único vestígio é console.error efêmero (4183-4184) — sem [observability] os logs não são retidos e audit() só grava em sucesso. A falha do job noturno fica invisível e só é descoberta como drift (leads não movidos) dias depois.
- **Critérios de aceite**:
  - [ ] ensureSchema (src/index.ts:454) passa a criar a tabela tick_log(id, ts, status, erro, processados, criados, movidos, erros); verificável em D1 local: `wrangler d1 execute kommo-cnn-db --local --command "SELECT sql FROM sqlite_master WHERE name='tick_log'"` retorna a definição com essas colunas.
  - [ ] Em wrangler dev local, 3 disparos de `curl "http://localhost:8787/__scheduled?cron=*+*+*+*+*"` (D1 íntegro) → `SELECT COUNT(*) FROM tick_log`=3, todas com status='ok', erro IS NULL e contadores >= 0; ou seja EXATAMENTE 1 linha por invocação (sem duplicar).
  - [ ] Forçando falha no D1 local (DROP TABLE fila_trabalho) e 1 disparo do __scheduled: o worker responde sem crashar (não relança) E `SELECT status,erro FROM tick_log ORDER BY id DESC LIMIT 1` = ('erro', <substring da mensagem da exceção>); com ALERT_WEBHOOK_URL apontando p/ capturador local, o tick com falha gera EXATAMENTE 1 POST (corpo contém a msg de erro) e o tick de sucesso gera 0 POST.
  - [ ] wrangler.toml passa a conter bloco `[observability]` com `enabled = true` e `npx wrangler deploy --dry-run --outdir <tmp>` conclui sem erro de validação de config.
- **Plano de teste**:
  - (a) repro: npx wrangler dev (D1 --local). Popular a fila com 1 item pendente (INSERT em fila_trabalho). Estado ATUAL: dropar fila_trabalho no D1 local e disparar `curl "http://localhost:8787/__scheduled?cron=*+*+*+*+*"` → a exceção só vira console.error; `SELECT * FROM tick_log` falha (tabela inexistente) e não há alerta — provando que a falha noturna é invisível/não-durável.
  - (b) prova: Após a correção, mesmo cenário de falha (fila_trabalho ausente) + ALERT_WEBHOOK_URL=http://localhost:9999 (capturador local): `SELECT status,erro FROM tick_log ORDER BY id DESC LIMIT 1`=('erro', msg) e o capturador registra 1 POST. Recriar fila_trabalho (ensureSchema roda no próximo tick) e disparar de novo → nova linha status='ok', erro IS NULL, 0 POST no capturador.
  - (c) regressão: Com D1 íntegro: (a) `curl /debug-selftest?mode=logic` continua 100% passando, incluindo INV1 (subreq <= 50/tick) — a escrita de tick_log + eventual alerta contam no budget; (b) um tick normal grava EXATAMENTE 1 linha 'ok' (`SELECT COUNT(*) FROM tick_log`=1 por invocação) e os contadores batem com o retorno de consumirFila; (c) `wrangler deploy --dry-run` valida a config com o novo [observability].
- **Dependências**: nenhuma
- **Risco do deploy**: Baixo. Tabela tick_log é aditiva (CREATE TABLE IF NOT EXISTS via ensureSchema, que já roda a cada entrada). Acrescenta 1 write D1 por invocação e, só em falha, 1 POST HTTP para ALERT_WEBHOOK_URL — ambos contam no teto de 50 subreq/invocação, mas a escrita ocorre no fim do tick e o alerta é raro. Nenhuma escrita em CNN/Kommo/WhatsApp. Raio de dano do alerta: 1 POST para a URL de alerta configurada (nada mais). [observability] apenas habilita retenção de logs (incluída no Free). Reversível.
- **Rollback**: Redeploy do bundle anterior (b0167bfa) — reverte scheduled()/wrangler.toml. tick_log pode ficar (inócua) ou `DROP TABLE tick_log`. Remover ALERT_WEBHOOK_URL dos secrets e o bloco [observability] do wrangler.toml. Nenhum estado persistente perigoso.
- **Esforço**: M | **Status**: backlog
- **Notas**: Design: (1) tick_log = 1 linha/tick com ts, status('ok'|'erro'), erro (msg+stack truncados ~300ch, espelhando filaMarcarErro), e contadores vindos do out de consumirFila (processados/criados/movidos/erros) + flags de quais produtores rodaram no minuto. (2) Envolver o corpo do scheduled (4158-4186) com try/catch/finally que SEMPRE persiste a linha, inclusive quando lança. (3) Helper de alerta: POST para env.ALERT_WEBHOOK_URL (secret, opcional — no-op se ausente) só no ramo de erro. (4) Opcional barato: endpoint read-only /debug-health que retorna último tick, último erro e gap desde o último 'ok' (permite detectar tick que parou de rodar). (5) Retenção: podar tick_log (ex.: DELETE WHERE ts < 14d) para não crescer sem limite. Teste local usa o endpoint __scheduled do wrangler dev e o TESTE Bruno não é necessário (não toca API externa a não ser o capturador de alerta local). INTERAÇÃO (não bloqueia): B1 (budget/subreqUsados global) — a escrita de tick_log e o alerta consomem subrequests; quando B1 for feito, garantir que essas chamadas sejam contabilizadas. Este item é ortogonal a A4/A6 (wrappers Kommo) e A5/C1 (lock da fila) e na verdade FACILITA depurá-los, por isso dependencias=[].

### F2 — Persistir sumário por tick (tick_resumo) + expor backlog da fila por tipo/status/idade
- **Origem**: achado F2 (MEDIO) | Evidência: src/index.ts:4182 (retorno de consumirFila descartado no cron); 1964-1996 (out rico montado — processados/criados/movidos/adiados/adiados_expirados/sem_acao/erros/parou_orcamento/itens[] — e `return out` em 1995, nunca persistido); 542 (filaMarcarErro grava ultimo_erro=msg.slice(0,300), sobrescreve a cada tentativa e trunca); 545-550 (filaStats agrupa só por status, sem tipo nem idade); 3868-3875 (/debug-audit expõe só filaStats). CONFERE (linhas idênticas).
- **Problema**: O cron descarta o resultado de consumirFila e nada é gravado por tick, então é impossível reconstruir depois quantos itens/erros/quanta duração um tick teve; erros só vivem em fila_trabalho.ultimo_erro (truncado a 300 chars e sobrescrito a cada retentativa) e o backlog de adiados/pendentes por tipo é invisível sem poll manual. Dispara sempre que se precisa investigar a posteriori (ex.: "por que o backlog cresceu às 15h?").
- **Critérios de aceite**:
  - [ ] Existe tabela tick_resumo criada em ensureSchema (CREATE TABLE IF NOT EXISTS, colunas mínimas: ts, gatilhos, processados, criados, movidos, adiados, adiados_expirados, sem_acao, erros, parou_orcamento, ms, subreq, erros_json). Verificável: SELECT sql FROM sqlite_master WHERE name='tick_resumo' no D1 local retorna essas colunas.
  - [ ] Fidelidade + toda-invocação: rodar 3 /debug-tick?dry=0 no wrangler dev (2 com fila semeada, 1 com fila vazia) => SELECT COUNT(*) FROM tick_resumo == 3 (o de fila vazia grava linha com processados=0), e a última linha bate campo-a-campo (processados, criados, movidos, adiados, erros, ms, subreq) com o bloco `consumidor` do JSON de resposta do endpoint.
  - [ ] Histórico de erro não se perde: forçar 2 itens a falhar com mensagens distintas em 2 ticks => as 2 mensagens completas são recuperáveis do histórico persistido (tick_resumo.erros_json ou tabela de erros), i.e. SELECT retorna 2 mensagens diferentes — ao contrário de fila_trabalho.ultimo_erro que só mantém a última truncada.
  - [ ] Backlog visível: endpoint read-only (ex.: /debug-fila ou /debug-audit estendido) retorna, por (tipo,status), {n, idade_mais_antigo_s}. Semear no D1 local 3 pendente tipo A3 + 2 pendente tipo ORC com criado_em conhecido => resposta mostra A3/pendente n=3 e ORC/pendente n=2, com idade_mais_antigo_s dentro de ±3s do criado_em injetado.
- **Plano de teste**:
  - (a) repro: Local: `npx wrangler dev` + D1 local (`.dev.vars` com WEBHOOK_SECRET). (1) `wrangler d1 execute kommo-cnn-db --local` para semear fila_trabalho com 3 itens pendentes (2 A3 válidos + 1 com payload que força erro no consumidor). (2) `curl 'localhost:8787/debug-tick?dry=0&cap=10&budget=40'`. (3) `SELECT name FROM sqlite_master WHERE name='tick_resumo'` => 0 linhas (tabela não existe) e nenhum registro do tick => confirma que nada é reconstruível. (4) Forçar o mesmo item a errar com 2 mensagens distintas em 2 ticks e `SELECT ultimo_erro FROM fila_trabalho WHERE id=<X>` => só a 2ª mensagem sobrevive (truncada a 300) => confirma perda do histórico de erro.
  - (b) prova: Após implementar: (a) repetir os 3 ticks (2 com fila, 1 vazia) => `SELECT COUNT(*) FROM tick_resumo`==3 e comparar a última linha com o bloco `consumidor` do JSON (bate campo-a-campo). (b) 2 erros distintos em 2 ticks => `SELECT` no histórico retorna as 2 mensagens completas. (c) semear 3 A3 + 2 ORC pendentes com criado_em conhecido e `curl` no endpoint de backlog => grupos A3/pendente n=3, ORC/pendente n=2 e idade_mais_antigo_s dentro de ±3s. (d) simular falha do INSERT de tick_resumo (nome de coluna inválido temporário) => consumirFila ainda retorna e o tick não lança (insert em try/catch como audit()).
  - (c) regressão: `curl localhost:8787/debug-selftest?mode=logic` e `?mode=fuzz` continuam passando; `/debug-tick?dry=1` retorna o MESMO shape do bloco `consumidor` de antes (persistência não altera contagem/decisão) e a soma `processados` sobre uma fila fixa é idêntica ao baseline pré-mudança; contagem de linhas em `auditoria` por tick inalterada; `filaStats`/`/debug-audit` seguem respondendo. Apagar `.dev.vars` ao fim.
- **Dependências**: nenhuma
- **Risco do deploy**: Baixo/aditivo: tabela via CREATE TABLE IF NOT EXISTS (sem migração destrutiva). Adiciona ~1 INSERT D1 por tick (~1440/dia) — dentro do free tier, mas confirmar que D1 no mesmo isolate NÃO conta no teto de 50 subreq (não deve inflar subreqUsados). INSERT deve ficar em try/catch (padrão do audit(), ~576) para nunca quebrar o tick. Precisa de retenção/prune (ex.: manter últimos N dias ou N linhas) para não crescer indefinidamente no D1 Free.
- **Rollback**: Remover a chamada de persistência no cron/consumirFila e o endpoint de backlog; a tabela tick_resumo fica inerte (sem impacto em outras tabelas) ou `DROP TABLE tick_resumo`. Redeploy do bundle anterior `b0167bfa` reverte tudo.
- **Esforço**: M | **Status**: backlog
- **Notas**: F2 é observabilidade — sem dependência de bloqueio: persiste o `out` que consumirFila JÁ produz, logo pode ir sozinho (dependencias=[]). Mas é COMPLEMENTAR à verificação dos achados de concorrência/idempotência: os contadores de tick_resumo (processados vs. itens realmente marcados feito, detecção de processado-2x) são a evidência binária que valida A5/C1 (fila sem lock) e B1 (budget global corrompido por ticks sobrepostos) — recomendo implementar F2 ANTES ou junto deles para poder medir as correções. A4/A6 (wrappers Kommo) não se cruzam com F2. Testes 100% D1 local + wrangler dev; raio de dano nulo (nenhuma chamada a Kommo/CNN/WhatsApp). Reaproveitar o padrão fault-isolado de audit() (~568-576). Escopo enxuto: 1 tabela + 1 insert no caminho do tick + 1 endpoint read-only + prune de retenção.

### BX — Polir lote de 6 fragilidades BAIXAS independentes (observabilidade + segurança + consistência)
- **Origem**: achado BX (BAIXO) | Evidência: src/index.ts:820-821 (orcamentosRecentes descarta APROVADO com dataAprovacao nula — filtro `o.dataAprovacao && ...`); 1924 vs 1925-1927 (decisao usa orcamentosRecentes filtrado, mas resumo/ultimo_status usa `orcs` cru → ultimo_status pode dizer APROVADO enquanto ultima_etapa reflete decisão que ignorou aprovação antiga); 572-576 (audit() com `catch { /* auditoria nunca quebra o fluxo */ }` vazio, engole erro sem log); 3771 e 3774 (webhookAuthOk/discoverAuthOk comparam segredo com `===`, não constante-time); handlers /debug sem resetSubreq — /debug-a2 (3799→syncKommoParaCnn), /debug-a3 (3805→syncCnnParaKommo), /debug-a4 (3814→backfillCadastros), /debug-f2 (3824→cronVespera), /debug-verificar (4050), /debug-auditoria (4056), /debug-split-colisao (4062) NÃO chamam resetSubreq no dispatcher, enquanto /debug-orcamento (4030) e /debug-orcamento-impacto (4036) chamam — inconsistente, budget global `subreqUsados` (53) vaza entre chamadas no mesmo isolate; churn ORC: produtorOrcamento (1823) re-enfileira por cursor CRIACAO + APROVACAO diariamente sem cache negativo → maioria drena sem_mudanca (consumirItemOrcamento).
- **Problema**: Seis fragilidades pequenas e independentes (sem perda de dado grave) somam ruído/risco: (1) audit() engole erros de INSERT sem log; (2) ultimo_status pode contradizer ultima_etapa em orcamento_sync; (3) comparação de segredo de auth não é constante-time; (4) vários handlers /debug não resetam o budget global subreqUsados (vaza no isolate quente); (5) APROVADO com dataAprovacao nula é descartado sem decisão explícita/documentada; (6) sweep ORC re-enfileira diariamente sem cache negativo. Disparam em operação normal (auth em cada webhook, audit em cada move, /debug em cada diagnóstico, ORC a cada tick).
- **Critérios de aceite**:
  - [ ] APROVADO/dataAprovacao nula com decisão explícita: `/debug-selftest?mode=logic` (local, wrangler dev) inclui novo caso com `{status:'APROVADO', dataAprovacao:null}` e o resultado bate com a política escolhida documentada no comentário de orcamentosRecentes (index.ts:814-822); selftest retorna `ok:true` (exit 0).
  - [ ] Consistência ultimo_status↔ultima_etapa: após consumir (dry=0 local) um paciente cujo único APROVADO tem dataAprovacao > 60d, `SELECT ultimo_status, ultima_etapa FROM orcamento_sync WHERE paciente_id_cnn=<pid>` NÃO retorna a combinação contraditória (ultimo_status='APROVADO' com ultima_etapa de etapa não-aprovada) — resumo passa a derivar do MESMO conjunto orcamentosRecentes usado por decisao (index.ts:1924-1927).
  - [ ] audit() observável: forçando erro de INSERT (ex.: DROP TABLE auditoria em D1 local) e chamando uma rota que audita, o `wrangler tail` mostra >=1 console.warn do audit E a resposta HTTP ainda é 200 (0 exceções propagadas ao fluxo).
  - [ ] Auth constante-time + resetSubreq consistente: (a) revisão confirma webhookAuthOk/discoverAuthOk (index.ts:3771/3774) usando comparação de tempo constante e teste manual: segredo correto→200, incorreto→401; (b) toda rota /debug que faz leitura Kommo/CNN chama resetSubreq() antes (grep: /debug-a2,-a3,-a4,-f2,-verificar,-auditoria,-split-colisao passam a ter resetSubreq), comprovado por 2 GETs seguidos a `/debug-a3?dry=1&max=1` no mesmo isolate (wrangler dev) retornando o MESMO subreq_total (não crescente).
- **Plano de teste**:
  - (a) repro: Local `npx wrangler dev` + D1 local (`.dev.vars` com WEBHOOK_SECRET). (audit) DROP TABLE auditoria; chamar rota que audita → hoje falha some silenciosa (nada no `wrangler tail`). (ultimo_status) consumir paciente com único APROVADO ~90d → `SELECT` mostra ultimo_status='APROVADO' mas ultima_etapa de etapa não-aprovada (contraditório). (resetSubreq) 2x `GET /debug-a3?dry=1&max=1` no mesmo isolate → subreq_total do 2º > do 1º (contador vazou). (auth) revisão: `===` curto-circuita no 1º byte divergente (timing observável).
  - (b) prova: (1) selftest logic com caso APROVADO/dataAprovacao=null → `ok:true`. (2) `SELECT ultimo_status,ultima_etapa` sem combinação contraditória. (3) erro forçado no audit → >=1 warn no `wrangler tail` e HTTP 200. (4a) segredo errado→401, correto→200 e revisão confirma comparação constante-time. (4b) 2x `/debug-a3?dry=1` → subreq_total idêntico. Cada prova é local (wrangler dev + D1 local) ou dry-run.
  - (c) regressão: `/debug-selftest?mode=logic|fuzz|stress` verde (decidirEtapaOrcamento intacto — pré-filtro só muda entrada). `/debug-tick?dry=1` mantém mesmos contadores/decisões (nenhum move novo introduzido). Webhooks W1/W2 com secret correto continuam 200 (auth não regrediu). Raio de dano: todos os testes rodam local ou dry-run read-only; se validar na edge usar TESTE Bruno (pid 28146949, tel 11946800329) com dry=1; CNN produção só leitura (§7.8), nenhum WhatsApp/move de lead vivo.
- **Dependências**: B1
- **Risco do deploy**: Baixo — mudanças pequenas e localizadas. Únicos pontos com risco de comportamento: (a) a política de APROVADO/dataAprovacao nula pode alterar quantos leads reativam → validar antes com `/debug-orcamento?impacto=1` (read-only); (b) a comparação constante-time NÃO muda a semântica de auth (mesmo resultado 200/401). resetSubreq nos /debug só afeta diagnósticos (o cron já reseta em scheduled() ~4159), não muda produção. Nenhuma migração de schema.
- **Rollback**: Redeploy do bundle anterior `b0167bfa` via `npx wrangler deploy` (rollback conhecido no CLAUDE.md). Sem migração de schema e sem novo estado persistido → rollback imediato e sem perda de dados; orcamento_sync já existente permanece compatível.
- **Esforço**: M | **Status**: backlog
- **Notas**: Checklist dos 6 sub-itens (independentes; priorizar por observabilidade/segurança): [1] audit() log em falha (index.ts:572-576) — observabilidade, P; [2] auth constante-time (3771/3774) — segurança, P; [3] resetSubreq consistente nos /debug (a2/a3/a4/f2/verificar/auditoria/split-colisao) — correção de diagnóstico, P; [4] ultimo_status derivar de orcamentosRecentes p/ não contradizer ultima_etapa (1924-1927) — consistência, P; [5] decisão explícita+documentada p/ APROVADO com dataAprovacao nula (820-821) — hoje descarta silenciosamente, P (validar impacto com /debug-orcamento?impacto=1). DEPENDÊNCIA: apenas o sub-item [3] (resetSubreq) acopla a B1 — se B1 re-escopar subreqUsados de global-de-módulo para per-invocation, o sub-item [3] é absorvido/muda de forma; fazer [3] junto/depois de B1 pra evitar retrabalho. Os outros 5 sub-itens NÃO dependem de nada e podem ir a qualquer momento. SUB-ITEM 6 (churn diário ORC / cache negativo em produtorOrcamento ~1823) foi DEIXADO DE FORA deste lote: é design (acopla à cadência da fila/produtor e às séries C/fila), critério binário exige medir baseline antes (ex.: `SELECT count(*) FROM fila_trabalho WHERE tipo='ORC'` re-enfileirados/dia vs sem_mudanca) — abrir ticket próprio e medir com /debug-count antes de agir, senão vira G. Manter este BX em M (5 sub-itens P + testes); se somar o sub-item 6, quebrar em BX-a (polish, M) e BX-b (negative-cache ORC, ticket separado).
